# 实验步骤：消融阶段 0 正式 Baseline

> 用途：在 RTX 5090 云服务器上完成 data-v2 源码消融实验的 `000 Baseline`。
> 本文件只规定操作顺序和命令。助手不启动任何正式训练；全部训练命令由用户手动输入。

## 0. 本次任务概要

| 项目 | 冻结值 |
|---|---|
| Git 分支 | `cloud/data-v2-5090` |
| 正式 Run ID | `data-v2-abl-000-y26m-b16-s42` |
| 10 epoch 预检 Run ID | `data-v2-abl-000-y26m-b16-s42-preflight10` |
| 模型起点 | 官方 `yolo26m-seg.pt` |
| 数据集 | `rice-pest-data-v2`，云端目录 `/root/yolo_data` |
| 正式配置 | `experiments/data-v2-abl-000-y26m-b16-s42.yaml` |
| 源码改进 | 无 Attention、无 Dice、无 P2Head |
| 训练 | epochs=300，patience=100，batch=16，nbs=64 |
| 输入与随机性 | imgsz=640，seed=42，deterministic=true |
| 优化器 | AdamW，lr0=0.001667，lrf=0.01，momentum=0.9，weight_decay=0.0005 |
| 掩膜与增强 | mask_ratio=2，mixup=0，mosaic=1，copy_paste=0.3 |
| 模型选择 | Val 上的 Ultralytics 分割 official fitness |

`nbs=64` 是 Ultralytics 的名义 batch size；实际 `batch=16` 时，训练器会按约 4 次梯度累积
保持等效优化尺度。配置中不额外写非官方的 `accumulate` 参数。

本轮从官方预训练权重重新开始。参数优化 P2
`data-v2-tune-mr2-nomix-e300-b16-s42` 只负责确定训练配方，其 `best.pt` 不作为正式
Baseline 的起点或结果。

---

## 1. 云端拉取阶段 0 提交

```bash
cd /root/yolo_plus
git switch cloud/data-v2-5090
git pull --ff-only
git branch --show-current
git log --oneline -1
```

分支应为 `cloud/data-v2-5090`，且仓库中应存在：

```bash
ls experiments/data-v2-abl-000-y26m-b16-s42.yaml
ls experiments/yolo_data_v2_cloud.yaml
```

若 `git pull` 超时，不要在云端手工重写配置；先解决 GitHub 网络连接，再继续。

## 2. 确认 data-v2 挂载位置

```bash
ls /root/yolo_data/images/train
ls /root/yolo_data/labels/train
```

本阶段只确认目录存在，不重新划分数据，不修改标签，也不切换到 Test。

## 3. 打印正式配置，不训练

```bash
python scripts/train_yolo26_seg.py \
  --config experiments/data-v2-abl-000-y26m-b16-s42.yaml \
  --run-name "data-v2-abl-000-y26m-b16-s42" \
  --dry-run
```

重点核对输出：

```text
model=yolo26m-seg.pt
epochs=300, patience=100, batch=16, nbs=64, imgsz=640
optimizer=AdamW, lr0=0.001667, lrf=0.01
warmup_epochs=3, warmup_bias_lr=0.1
mask_ratio=2, mosaic=1.0, mixup=0.0, copy_paste=0.3
degrees=15, flipud=0.5, fliplr=0.5, scale=0.3
seed=42, deterministic=true, split=val
```

输出中不应出现自定义 P2Head 的语义权重迁移、CBAM 或 Dice 配置。

## 4. 用户手动运行 10 epoch 预检

阶段 0 不再运行 1 epoch；只进行一次 10 epoch 流程预检：

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-000-y26m-b16-s42.yaml \
  --run-name "data-v2-abl-000-y26m-b16-s42-preflight10" \
  --preflight10
```

预检通过条件：

- 正确加载 `yolo26m-seg.pt` 和 data-v2；
- GPU 为预期 RTX 5090，batch=16 无 OOM；
- 10 个 epoch 的前向、反向、Val 和权重保存全部完成；
- 训练 loss 与主要 Box/Mask 指标保持有限值，没有模型权重 NaN/Inf；
- 生成独立目录 `runs/data-v2-abl-000-y26m-b16-s42-preflight10/`。

预检指标只用于排错，不进入论文表格、`comparison.csv` 或正式 Baseline 结论。若预检失败，
保留日志并停止，不要直接启动 300 epoch。

## 5. 用户手动启动 300 epoch 正式训练

前台运行：

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-000-y26m-b16-s42.yaml \
  --run-name "data-v2-abl-000-y26m-b16-s42"
```

推荐使用后台运行，避免 SSH 断开中止训练：

```bash
nohup python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-000-y26m-b16-s42.yaml \
  --run-name "data-v2-abl-000-y26m-b16-s42" \
  > train_data-v2-abl-000-y26m-b16-s42.log 2>&1 &
```

查看训练日志：

```bash
tail -f train_data-v2-abl-000-y26m-b16-s42.log
```

查看 GPU：

```bash
nvidia-smi
```

正式目录必须是：

```text
runs/data-v2-abl-000-y26m-b16-s42/
```

不要使用 `exist_ok=true` 覆盖同名目录。若目录已存在，先停止并核对，不要删除历史结果。

## 6. 训练结束后打包

确认训练进程已经结束，且目录中存在 `weights/best.pt`、`results.csv`、`args.yaml` 后执行：

```bash
python scripts/transfer_run.py pack \
  --run-id "data-v2-abl-000-y26m-b16-s42"
```

预期归档：

```text
exports/data-v2-abl-000-y26m-b16-s42.zip
```

不要在训练仍进行时打包。

## 7. 下载并在本地解包

将下面命令中的 `SCP_PORT` 和云服务器地址替换为当前实例控制台提供的值：

```bash
scp -P 15425 \
  root@connect.westb.seetacloud.com:/root/yolo_plus/train_data-v2-abl-000-y26m-b16-s42.log \
  ./runs/data-v2-abl-000-y26m-b16-s42
```

回到 Windows 本地项目后执行：

```powershell
python scripts/transfer_run.py unpack --archive "exports/data-v2-abl-000-y26m-b16-s42.zip"
```

## 8. 正式结果登记

完整 Run 解包到 `runs/data-v2-abl-000-y26m-b16-s42/` 后，再执行：

```powershell
python scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-000-y26m-b16-s42" `
  --run-id "data-v2-abl-000-y26m-b16-s42" `
  --data data-v2
```

随后：

1. 从 `experiment_records/runs/_template.md` 创建
   `experiment_records/runs/data-v2-abl-000-y26m-b16-s42.md`；
2. 记录 best epoch、official fitness、Box/Mask 总体与分类型指标、训练时间、显存、参数量和 GFLOPs；
3. 更新 `experiment_records/comparison.csv`；
4. 将正式 Baseline 与调参 P2 只作复现性核对，不把调参 P2 写入正式消融表；
5. 在 `000` 结果核验完成前，不开始 Dice、Attention 或 P2Head 的正式训练。

## 9. 评估边界

- 当前只使用 Val 进行权重和方案选择；
- Test 保留到最终 `Ours`、跨模型方案与推理阈值全部冻结后，只统一评估一次；
- 本阶段固定 seed=42，不补 seed=2、3，论文需声明没有多 seed 均值与标准差；
- 任何配置、源码、数据或 Run ID 变化都必须新建实验，不覆盖 `000`。
