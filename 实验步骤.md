# 实验步骤：全 C3k2-DRB + P3/P4 独立 SCSA

## 1. 本轮任务

| 项目 | 本轮值 |
|---|---|
| 分支 | `feature/data-v2-abl-drb-scsa` |
| 代码起点 | 正式 Baseline `c0f4f35` |
| 正式 Run ID | `data-v2-abl-drb-scsa-p34-b16-s42` |
| 配置 | `experiments/data-v2-abl-drb-scsa-p34-b16-s42.yaml` |
| 模型 | 全部 8 个 C3k2 使用 DRB；原第 4、6 层后独立加入残差 SCSA |
| 预训练 | 官方 `yolo26m-seg.pt`，自动按层号映射可复用权重 |
| 数据 | data-v2，`/root/yolo_data`，train/val/test = 938/117/118 |
| 配方 | 与正式 `000` 的全部 `train` 参数相同 |
| 训练 | RTX 5090，epochs=300，patience=100，batch=16，imgsz=640，seed=42 |
| 选择 | best.pt 使用官方 fitness；方案主指标为 Val Mask mAP50-95，同时报告 mAP50 |

本轮把 DRB 与 SCSA 作为一个整体结构候选。保留原始 C2PSA、末级 PSA、Segment26/Proto，
不叠加 Dice、P2Head、D1 或 DySample。原理和层号对应见
[教学文件](knowledge/全C3k2-DRB与P3P4-SCSA原理与实现.md)。

## 2. 云服务器：拉取新分支

在原来的训练环境中执行：

```bash
cd /root/yolo_plus
git status --short
git fetch origin
git switch feature/data-v2-abl-drb-scsa
git pull --ff-only
git branch --show-current
git log -1 --oneline
```

如果 `git status` 有未提交的源码或配置修改，先保存并处理这些修改再切分支，不使用强制覆盖。
分支输出必须是 `feature/data-v2-abl-drb-scsa`。

只有新服务器、尚无 `/root/yolo_plus` 时，才用下面这组克隆命令替代上面一组：

```bash
cd /root
git clone --branch feature/data-v2-abl-drb-scsa https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd /root/yolo_plus
```

确认数据挂载和 GPU：

```bash
ls -d /root/yolo_data/images/train /root/yolo_data/images/val
ls -d /root/yolo_data/labels/train /root/yolo_data/labels/val
nvidia-smi
```

保持原来的数据划分和 CUDA PyTorch 环境。

## 3. 云服务器：配置与 CUDA 检查

先读取本轮配置。此命令会检查运行依赖，但不训练：

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-drb-scsa-p34-b16-s42.yaml \
  --run-name data-v2-abl-drb-scsa-p34-b16-s42 \
  --dry-run
```

输出的模型应为 `yolo26m-drb-scsa-seg.yaml`，`[PRETRAINED]` 为 `yolo26m-seg.pt`。
核心参数为 batch=16、epochs=300、imgsz=640、AdamW、lr0=0.001667、mask_ratio=2、mixup=0。

再执行无数据集的 CUDA 检查：

```bash
python scripts/check_drb_scsa.py --weights yolo26m-seg.pt --cuda
```

首次使用官方权重会自动下载。应显示 `Ran 7 tests` 和 `OK`。
该检查验证结构、权重迁移、CUDA AMP 反向、FP16 推理及融合，不会开始正式训练。

## 4. 云服务器：10 epoch 预检

```bash
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-drb-scsa-p34-b16-s42.yaml \
  --run-name data-v2-abl-drb-scsa-p34-b16-s42-preflight10 \
  --preflight10
```

确认 batch=16 无 OOM、训练与 Val 能完成、权重能够保存和重新验证。若出现训练 loss
持续 NaN、CUDA 报错或权重异常，保留报错并停止，不进入正式训练。
预检不填任何实验表、不进入 `comparison.csv`，也不创建单次分析。

## 5. 云服务器：正式训练并保存日志

先创建 tmux 会话：

```bash
tmux new -s drb_scsa
```

在新会话中执行一次：

```bash
cd /root/yolo_plus
set -o pipefail
mkdir -p /root/autodl-tmp/log_file
git rev-parse HEAD | tee -a /root/autodl-tmp/log_file/train_data-v2-abl-drb-scsa-p34-b16-s42.log
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-drb-scsa-p34-b16-s42.yaml \
  --run-name data-v2-abl-drb-scsa-p34-b16-s42 \
  2>&1 | tee -a /root/autodl-tmp/log_file/train_data-v2-abl-drb-scsa-p34-b16-s42.log
```

先按 `Ctrl+B`，松开后按 `D`，可退出会话而不停止训练。重新查看：

```bash
tmux attach -t drb_scsa
```

也可以在另一 SSH 窗口查看日志：

```bash
tail -f /root/autodl-tmp/log_file/train_data-v2-abl-drb-scsa-p34-b16-s42.log
```

正式输出目录为 `runs/data-v2-abl-drb-scsa-p34-b16-s42/`。已经存在时不要覆盖；
重跑必须使用新的 Run ID。预检结束后，本次正式训练仍从官方权重开始，不接着预检训练。
达到 patience=100 后提前停止是允许的，保存和比较官方 `best.pt`。

## 6. 云服务器：日志放入 Run 后一起打包

训练结束并完成 `best.pt` 验证后执行：

```bash
cd /root/yolo_plus
cp -n /root/autodl-tmp/log_file/train_data-v2-abl-drb-scsa-p34-b16-s42.log \
  runs/data-v2-abl-drb-scsa-p34-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-drb-scsa-p34-b16-s42
```

生成 `exports/data-v2-abl-drb-scsa-p34-b16-s42.zip`。ZIP 已包含训练日志，无需再单独下载日志。

## 7. Windows 本地：SCP 下载并解包

以下命令在本地 PowerShell 中执行，不在云服务器执行。主机和端口填写当前实例控制台
SSH 命令中的值，例如只填写主机名，不带 `root@`，端口只填写数字。

```powershell
Set-Location -LiteralPath 'E:\study\graduate_sec\论文撰写\模型训练'
$sshHost = Read-Host '当前云服务器 SSH 主机名'
$sshPort = Read-Host '当前云服务器 SSH 端口'
$runId = 'data-v2-abl-drb-scsa-p34-b16-s42'
New-Item -ItemType Directory -Force -Path '.\exports' | Out-Null
scp -P "$sshPort" "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" '.\exports\'
```

SCP 下载成功后再执行：

```powershell
python scripts/transfer_run.py unpack --archive "exports/${runId}.zip"
```

完整结果保存在 `runs/data-v2-abl-drb-scsa-p34-b16-s42/`。已有同名 ZIP 或 Run 时先核对，
不覆盖此前结果。本地保持本分支，确保加载自定义权重时能找到 DRB/SCSA 类。

## 8. 本地：正式结果分析

回传完整 Run 后再登记；当前不预填性能：

```powershell
python scripts/fill_results_table.py `
  --run-dir 'runs/data-v2-abl-drb-scsa-p34-b16-s42' `
  --run-id 'data-v2-abl-drb-scsa-p34-b16-s42' `
  --data data-v2
```

在 `experiment_records/runs/data-v2-abl-drb-scsa-p34-b16-s42.md` 写分析，并回填总表表 18。
比较 `000 Baseline` 的 Mask mAP50-95=0.36348、mAP50=0.71132，检查两类指标、
训练时间、显存与实际模型开销。完整候选确认有效后，再讨论与 D1 的组合，不自动安排额外消融。

本轮固定 seed=42，只使用 Val 选方案。Test 留到最终方案冻结后统一评估。
