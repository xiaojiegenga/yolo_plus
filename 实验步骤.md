# 实验步骤：改进 F2（局部特征引导的条带门控）

> 更新：2026-09-14。F2 源码、配置和本地验证已完成，正式精度待测。本轮仅训练独立 F2，沿用冻结配方。

## 1. 本次实验

| 项目 | 值 |
|---|---|
| 云端分支 | `feature/data-v2-abl-f2-localgate` |
| 代码版本 | 云端拉取后用 `git log -1 --oneline` 核对；将完整提交号记入训练日志 |
| 正式 Run ID | `data-v2-abl-f2-localgate-b16-s42` |
| 配置 | `experiments/data-v2-abl-f2-localgate-b16-s42.yaml` |
| 模型 | `yolo26m-p3-localgate-seg.yaml`；Neck 第 16 层 `C3k2LocalGate` |
| 结构 | 保持 F 的 P3 三分支；局部 3×3 特征产生横纵两个空间门；门初值为 1，投影零初始化 |
| 配方 | 与 000 相同；300 epoch、patience=100、batch=16、imgsz=640、seed=42、mask_ratio=2、AMP |
| 对照 | `data-v2-abl-000-y26m-b16-s42`；Val Mask mAP50-95 = 0.36348 |
| 初始化 | 官方 `yolo26m-seg.pt` |
| 云端数据 / GPU | `/root/yolo_data` / RTX 5090 |

原理、张量尺寸、理论来源与源码说明见 [F2 教材](knowledge/改进F2-局部引导条带门控原理与实现.md)。当前总体分析分支维护说明和结果；模型、训练配置与测试文件位于 F2 独立分支。

## 2. 云端拉取 F2

在现有服务器仓库中执行：

```bash
cd /root/yolo_plus
git remote set-branches --add origin feature/data-v2-abl-f2-localgate
git fetch origin
git switch feature/data-v2-abl-f2-localgate
git pull --ff-only origin feature/data-v2-abl-f2-localgate
git log -1 --oneline
git status --short
```

`set-branches --add` 将 F2 加入远端抓取范围，也适用于原先只克隆了单个分支的仓库。首次切换会建立本地 F2 分支；`git status --short` 应没有输出。

新服务器首次克隆可执行：

```bash
cd /root
git clone --branch feature/data-v2-abl-f2-localgate https://github.com/xiaojiegenga/yolo_plus.git
cd yolo_plus
```

## 3. 读取训练参数

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-f2-localgate-b16-s42.yaml \
  --run-name data-v2-abl-f2-localgate-b16-s42 --dry-run
```

输出应指向 F2 模型 YAML、官方 `yolo26m-seg.pt` 和正式 F2 Run 目录，训练参数与第 1 节一致。`--dry-run` 读取并显示参数，不启动训练。

本地 F2 7 项检查与原 F 6 项回归通过；另已验证官方权重 80→2 类迁移和 640 初始预测一致。F2 两类模型未融合参数 27,039,656，融合参数 23,576,660；第 16 层应为 C3k2LocalGate。云端训练、Val、AMP 和显存通过下一步预检确认。

## 4. 10 epoch 短预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-f2-localgate-b16-s42.yaml \
  --preflight10
```

预检自动使用带 `preflight` 和时间的独立 Run ID。确认训练及 Val 正常完成、损失有限、显存可用。10 epoch 预检不进入论文精度排名，不填参数优化表或 `comparison.csv`。正式实验从官方权重重新开始。

## 5. tmux 正式训练

预检通过后创建会话：

```bash
tmux new -s f2-localgate
```

进入 tmux 后执行：

```bash
cd /root/yolo_plus
set -o pipefail
git rev-parse HEAD | tee -a train_data-v2-abl-f2-localgate-b16-s42.log
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-f2-localgate-b16-s42.yaml \
  --run-name data-v2-abl-f2-localgate-b16-s42 2>&1 | tee -a train_data-v2-abl-f2-localgate-b16-s42.log
```

按 `Ctrl+B`，松开后按 `D`，可离开 tmux，训练继续运行。重新进入：

```bash
tmux attach -t f2-localgate
```

正式输出目录为 `runs/data-v2-abl-f2-localgate-b16-s42/`。重跑使用新 Run ID，并同步修改日志名、打包和解包命令中的 ID；训练入口会阻止覆盖已有 Run。

## 6. 打包回传

训练正常完成后，在云端将日志一并收入 Run：

```bash
cp -n train_data-v2-abl-f2-localgate-b16-s42.log runs/data-v2-abl-f2-localgate-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-f2-localgate-b16-s42
```

得到 `exports/data-v2-abl-f2-localgate-b16-s42.zip`。在本地 PowerShell 的项目根目录执行 SCP，SSH 主机和端口填写当前云实例控制台给出的值：

```powershell
$sshHost = Read-Host '云服务器 SSH 主机'
$sshPort = Read-Host '云服务器 SSH 端口'
$runId = 'data-v2-abl-f2-localgate-b16-s42'
New-Item -ItemType Directory -Path '.\exports' -Force | Out-Null
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" `
  '.\exports\'
```

ZIP 下载完成后，激活本机 Python 环境（例如当前 `(yolo)` 环境），在同一项目根目录解包：

```powershell
python scripts/transfer_run.py unpack `
  --archive "exports/${runId}.zip"
```

本地已有同名 Run 时不重复解包。训练脚本末尾的旧 SCP 地址仅是历史提示；下载使用当前云实例实际 SSH 地址和端口、本机当前 exports 路径。完整 Run 保存在 `runs/`，代码仓库只同步轻量记录。

## 7. 结果返回后

按官方 `Box mAP50-95 + Mask mAP50-95` fitness 选择 `best.pt`，比较该权重对应的 Val Mask mAP50-95，同时报告 Mask mAP50、分类别 AP、小目标指标与负样本误检。

| 对比 | 判断依据 |
|---|---|
| F2 对 Baseline | 官方 best.pt 的 Val Mask mAP50-95 >0.36348，登记为 seed=42 独立正向候选 |
| F2 对原 F | 同时报告相对原 F 的 0.35882 的差异及小卷叶螟表现 |

D1+F2 由独立 F2 正式结果决定，本轮不安排组合训练。10 epoch 预检只确认运行，不用于判断改进成功。

正式结果由本地根据完整 Run 回填 `experiment_records/comparison.csv`、单次 Run 记录及实验总表。设计、原理与教材维护在 `knowledge/`。Test 留到最终方案冻结后统一评估。
