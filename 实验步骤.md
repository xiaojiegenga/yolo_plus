# 实验步骤：改进 C（轻量 P2Head）

> 更新：2026-09-07。C 代码、配置、5 项本地测试与官方预训练权重迁移检查已完成。
> 当前待办：RTX 5090 云端 10 epoch 预检 → 用户手动正式训练 → 回传至本地总体分析分支。
> 本页保存在 A 总体分析分支和 C 实现分支；云端执行 C 训练时必须使用 C 分支。

## 0. 实验身份

| 项目 | 固定值 |
|---|---|
| 总体分析分支 | `feature/data-v2-abl-a-attention` |
| 云端训练分支 | `feature/data-v2-abl-c-p2head`，从正式 Baseline `c0f4f35` 创建 |
| 正式 Run ID | `data-v2-abl-001-p2head-b16-s42` |
| 预检 Run ID | `data-v2-abl-001-p2head-b16-s42-preflight10` |
| 配对基准 | `data-v2-abl-000-y26m-b16-s42` |
| 模型 YAML | `ultralytics-main/ultralytics/cfg/models/26/yolo26m-p2lite-seg.yaml` |
| 训练配置 | `experiments/data-v2-abl-001-p2head-b16-s42.yaml` |
| 初始化 | 官方 `yolo26m-seg.pt`；新增参数 seed=42 |
| 唯一结构变量 | 轻量 P2 Neck 旁路及 P2 Box/Cls/Mask coefficient 分支 |
| Proto | 保留 P3/P4/P5 融合；640 输入时为 32×160×160 |
| 训练配方 | 300 epoch、patience=100、batch=16、imgsz=640、seed=42，其余与 000 相同 |
| 已测结构成本 | 2 类 fused Params=23,757,752；GFLOPs@640=133.213389 |

原理、层号和迁移规则见 [C 知识文档](knowledge/改进C-P2Head小目标分支原理与实现.md)。
A1、A2 和 B 已完成，本次为 C 独立消融。

## 1. 云端拉取 C 分支

已有 `/root/yolo_plus` 仓库时执行：

```bash
cd /root/yolo_plus
git fetch origin
git switch feature/data-v2-abl-c-p2head
git pull --ff-only
git branch --show-current
git log -1 --oneline
git status --short
```

首次切换时 Git 通常自动创建远端跟踪；若提示本地分支不存在，可执行：

```bash
git switch --track origin/feature/data-v2-abl-c-p2head
```

尚无仓库时执行：

```bash
cd /root
git clone --branch feature/data-v2-abl-c-p2head https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd /root/yolo_plus
```

当前分支应为 `feature/data-v2-abl-c-p2head`，`git status --short` 应无输出。
若有本地修改，先保存再切换；不要覆盖服务器已有 Run。

## 2. 数据与最终配置

```bash
cd /root/yolo_plus
ls experiments/data-v2-abl-001-p2head-b16-s42.yaml
ls ultralytics-main/ultralytics/cfg/models/26/yolo26m-p2lite-seg.yaml
ls /root/yolo_data/images/train /root/yolo_data/images/val
ls /root/yolo_data/labels/train /root/yolo_data/labels/val
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-001-p2head-b16-s42.yaml \
  --run-name data-v2-abl-001-p2head-b16-s42 \
  --dry-run
```

云入口保留镜像内 PyTorch，只在其他依赖缺失时安装仓库依赖。Dry-run 不构建模型、
不下载权重、不启动训练。检查 `[MODEL]` 为 `yolo26m-p2lite-seg.yaml`，
`[PRETRAINED]` 为 `yolo26m-seg.pt`；epochs=300、batch=16、imgsz=640、seed=42、
mask_ratio=2、mixup=0、optimizer=AdamW；输出目录与正式 Run ID 相同。

## 3. 云端结构和官方权重检查

```bash
python scripts/check_p2head.py --weights yolo26m-seg.pt
```

官方权重不存在时会下载。该命令使用 CPU 合成输入，不读取数据集、不创建训练 Run。
预期 5 项测试通过；官方 80 类权重到 2 类 C 模型迁移 890/1078 个状态张量，
其余为新增 P2 张量和形状变化的类别末层。检查 stride=4/8/16/32、Proto=160×160，
并核对末尾模型规模与第 0 节一致。若检查失败，先回传报错，修复后再预检。

## 4. 10 epoch 预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-001-p2head-b16-s42.yaml \
  --run-name data-v2-abl-001-p2head-b16-s42-preflight10 \
  --preflight10
```

检查模型末层为 `Segment26P2Lite`，batch=16 无 OOM，训练/Val/保存均完成，
损失与指标无持续 NaN/Inf；记录峰值显存和单轮时间。若显存不足，不在云端临时减小 batch，
先回传日志，再在本地重新决定实验配置和 Run ID。
预检不写入 `comparison.csv`、正式总表或论文精度排名。

## 5. 用户手动启动正式训练

预检正常后，在云端手动执行以下命令。首行将实际训练 commit 写入日志。

```bash
cd /root/yolo_plus
nohup bash -c '
  git rev-parse HEAD
  exec python -u scripts/cloud_train_data_v2.py \
    --config experiments/data-v2-abl-001-p2head-b16-s42.yaml \
    --run-name data-v2-abl-001-p2head-b16-s42
' > train_data-v2-abl-001-p2head-b16-s42.log 2>&1 &
```

查看日志和显存：

```bash
tail -f train_data-v2-abl-001-p2head-b16-s42.log
```

按 `Ctrl+C` 退出日志查看，训练继续。

```bash
watch -n 3 nvidia-smi
```

正式输出为 `runs/data-v2-abl-001-p2head-b16-s42/`。同名目录已存在时入口会停止，
重跑需更换 Run ID；日志也使用新名称。不要覆盖既有日志。

## 6. 训练完成后打包完整 Run

```bash
ls runs/data-v2-abl-001-p2head-b16-s42/args.yaml
ls runs/data-v2-abl-001-p2head-b16-s42/results.csv
ls runs/data-v2-abl-001-p2head-b16-s42/weights/best.pt
ls runs/data-v2-abl-001-p2head-b16-s42/weights/last.pt
cp -n train_data-v2-abl-001-p2head-b16-s42.log runs/data-v2-abl-001-p2head-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-001-p2head-b16-s42
```

先确认日志有正常训练结束、best.pt 验证和结果保存信息。正常 EarlyStopping 可以少于
300 epoch；异常中断需先诊断。打包生成 `exports/data-v2-abl-001-p2head-b16-s42.zip`，
其中包含训练日志，预检目录独立保留。

## 7. Windows 本地接收并回到总体分析分支

在本地 PowerShell 项目根目录执行：

```powershell
git switch feature/data-v2-abl-a-attention
git pull --ff-only
$env:YOLO_CONFIG_DIR = Join-Path (Get-Location) '.cache\ultralytics'
$env:PYTHONUTF8 = '1'
$sshHost = '替换为当前云服务器地址'
$sshPort = 12345
$runId = 'data-v2-abl-001-p2head-b16-s42'
New-Item -ItemType Directory -Force exports | Out-Null
scp -P $sshPort "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" .\exports\
.\.venv\Scripts\python.exe scripts/transfer_run.py unpack --archive "exports/${runId}.zip"
```

A 分支维护结果表；其源码保留注意力实现。C checkpoint 需要 C 的类定义，因此需要用
`torch.load` 核验权重或执行独立 Val 时，先保存修改并切到 C 分支完成模型读取；随后切回 A
分支写正式分析。读取 CSV、日志与解包不需要切到 C 分支。

## 8. 在 A 分支登记和分析

```powershell
.\.venv\Scripts\python.exe scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-001-p2head-b16-s42" `
  --run-id "data-v2-abl-001-p2head-b16-s42" `
  --branch "feature/data-v2-abl-c-p2head" `
  --data data-v2
```

创建正式记录 `experiment_records/runs/data-v2-abl-001-p2head-b16-s42.md`，核对 CSV、
args、checkpoint 和日志中的训练 commit。总体指标按 official fitness 最佳 epoch 登记，
分类别指标注明 best.pt 验证来源，更新实验总表与 PROGRESS。

与 000 比较总体/两类别 AP、Recall、收敛、时间、显存，并按主计划固定的
letterbox-640 后 bbox 面积 <32×32 口径另做小卷叶螟评估。专项评估保持完整场景的匹配关系，
由独立评估记录保存，不更改官方总体 Validator 或权重选择规则；取得专项结果前不宣称小目标收益。

## 9. 固定边界

- 正式训练仅由用户手动启动；本地测试通过不能代替云端预检。
- 参数、数据和 Val 指标口径与 000 保持一致，Test 留到最终冻结后使用。
- 预检与正式 Run 分开，重跑使用新 Run ID。
- 总体分析在 A 分支；C 训练、checkpoint 读取与推理使用 C 分支代码。
- 完整 Run、权重、数据集与 ZIP 留在本地或外部存储，GitHub 只同步源码、配置与轻量记录。
