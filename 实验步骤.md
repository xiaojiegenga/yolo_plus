# 实验步骤：改进 D1（P2 高分辨率掩码原型）

> 更新：2026-09-08。D1 独立源码与训练配置已准备；正式训练待运行。

## 0. 实验身份

| 项目 | 固定值 |
|---|---|
| 云端训练分支 | `feature/data-v2-abl-d-p2proto`，基于正式 Baseline `c0f4f35` |
| D1 源码提交 | `4b66461` |
| 总体分析分支 | `feature/data-v2-abl-a-attention` |
| 正式 Run ID | `data-v2-abl-d1-p2proto-b16-s42` |
| 10 epoch 预检 Run ID | `data-v2-abl-d1-p2proto-preflight10-b16-s42` |
| 训练配置 | `experiments/data-v2-abl-d1-p2proto-b16-s42.yaml` |
| 对照 | `data-v2-abl-000-y26m-b16-s42` |
| 初始化 | 官方 `yolo26m-seg.pt`；模型构建前设置 seed=42 |
| 结构 | P2 细节注入 + 窄通道 stride-2 原型；检测 stride=8/16/32 |
| 训练配方 | 300 epoch、patience=100、batch=16、imgsz=640，其余与 000 相同 |

原理见 [D1 知识文档](knowledge/改进D1-P2高分辨率掩码原型原理与实现.md)。

## 1. 云端拉取

已有仓库：

```bash
cd /root/yolo_plus
git fetch origin
git switch feature/data-v2-abl-d-p2proto
git pull --ff-only
git log -1 --oneline
git status --short
```

首次切换时 Git 自动创建同名远端跟踪分支。尚无仓库：

```bash
cd /root
git clone --branch feature/data-v2-abl-d-p2proto https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd /root/yolo_plus
```

## 2. 配置和结构检查

```bash
ls /root/yolo_data/images/train /root/yolo_data/images/val
ls /root/yolo_data/labels/train /root/yolo_data/labels/val
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-d1-p2proto-b16-s42.yaml \
  --run-name data-v2-abl-d1-p2proto-b16-s42 --dry-run
python scripts/check_p2proto.py --weights yolo26m-seg.pt
```

Dry-run 应显示 D1 模型 YAML、官方预训练权重及冻结配方。结构检查使用 CPU 合成输入，
预期 8 项 PASS，并通过官方权重迁移和 2 类重建检查；无需安装 pytest。
2 类 D1 迁移 889/918 个状态张量，15 个形状变化张量、14 个新增张量。
训练入口的两次加载日志分别为 903/918（80 类初次迁移）和 904/918（D1 模型重建为 2 类，含新增参数的复制）；889/918 是最终模型中直接来自官方权重的张量数。
原型为 32×320×320，检测 stride 为 8/16/32。

## 3. 10 epoch GPU 预检

在正式长训前检查当前云端环境、AMP、batch=16 显存及真实数据训练：

```bash
set -o pipefail
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-d1-p2proto-b16-s42.yaml \
  --preflight10 --run-name data-v2-abl-d1-p2proto-preflight10-b16-s42 \
  2>&1 | tee -a train_data-v2-abl-d1-p2proto-preflight10-b16-s42.log
```

预检只检查运行兼容性，不进入正式排名或结果表。保持冻结 batch=16；若运行异常，保留日志后诊断。

## 4. tmux 正式训练

进入会话：

```bash
tmux new -s d1-p2proto
```

在会话内执行：

```bash
cd /root/yolo_plus
set -o pipefail
{
  git rev-parse HEAD
  python -u scripts/cloud_train_data_v2.py \
    --config experiments/data-v2-abl-d1-p2proto-b16-s42.yaml \
    --run-name data-v2-abl-d1-p2proto-b16-s42
} 2>&1 | tee -a train_data-v2-abl-d1-p2proto-b16-s42.log
```

先按 `Ctrl+B`，松开再按 `D`，离开会话并保持训练。返回：

```bash
tmux attach -t d1-p2proto
```

正式训练由用户手动启动。同名 Run 已存在时入口停止；重跑使用新的 Run ID 和日志名。

## 5. 完成后打包

日志应包含正常训练结束及 best.pt 验证结果；patience=100 正常早停可以少于 300 epoch。

```bash
cp -n train_data-v2-abl-d1-p2proto-b16-s42.log runs/data-v2-abl-d1-p2proto-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-d1-p2proto-b16-s42
```

得到 `exports/data-v2-abl-d1-p2proto-b16-s42.zip`。

## 6. 本地接收与分析

在本地 PowerShell 的 `E:\Study\claude_yolo_plus` 执行，替换当次实例地址与端口：

```powershell
$sshHost = '替换为当前云服务器地址'
$sshPort = 12345
$runId = 'data-v2-abl-d1-p2proto-b16-s42'
New-Item -ItemType Directory -Force exports | Out-Null
scp -P $sshPort "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" .\exports\
python scripts/transfer_run.py unpack --archive "exports/${runId}.zip"
```

checkpoint 读取与独立 Val 使用 D 分支源码；总体分析和表格在 A 分支维护。
核验 CSV、args、权重及日志中的训练 commit 后，登记正式结果：

```powershell
python scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-d1-p2proto-b16-s42" `
  --run-id "data-v2-abl-d1-p2proto-b16-s42" `
  --branch "feature/data-v2-abl-d-p2proto" --data data-v2
```

总体结果按 official fitness 最佳 epoch 登记，同时记录两类别 Mask AP、显存和耗时。
与 000 比较 Val Mask mAP50-95，Test 留待最终方案冻结后统一评估。
