# 实验步骤：改进 H（D1 + 局部候选分类精修）

> 更新：2026-09-15。H 源码与固定配置已准备，GPU 训练与正式 Val 待在云服务器执行。

## 1. 固定信息

| 项目 | 固定值 |
|---|---|
| 云端分支 | `feature/data-v2-abl-h-localrefine` |
| 父模型 | D1 `data-v2-abl-d1-p2proto-r2-b16-s42/weights/best.pt` |
| D1 Val Mask mAP50-95 | `0.37327` |
| D1 原图尺寸 Val Mask mAP50-95 | `0.34814` |
| H 正式 Run ID | `data-v2-abl-d1h-localrefine-rb128-s42` |
| 数据集 | `rice-pest-data-v2`，Train 生成候选，Val 选择模型 |
| GPU | RTX 5090 |

H 冻结 D1，只训练约 1.224M 参数的三分类 crop refiner。候选阈值为 `0.25`；同类 GT
框 IoU ≥ `0.5` 为对应虫类，与全部 GT 框 IoU < `0.1` 为背景，其余忽略。正式训练为
20 epochs、crop batch 128、AdamW、lr `1e-4`、weight decay `5e-4`、seed 42。

本地电脑只负责代码、静态/CPU 检查和结果接收，不运行训练或 CUDA 测速。Test 在最终方案
冻结前不使用。

## 2. 云端同步与前置文件

已有仓库：

```bash
cd "${HOME}/yolo_plus"
git status --short
git fetch origin
git switch feature/data-v2-abl-h-localrefine
git pull --ff-only
```

如果 `git status --short` 显示云端自有修改，先停止切换并人工处理，不覆盖云端文件。

首次克隆：

```bash
git clone --branch feature/data-v2-abl-h-localrefine \
  https://github.com/xiaojiegenga/yolo_plus.git "${HOME}/yolo_plus"
cd "${HOME}/yolo_plus"
```

确认 D1 权重和 data-v2 均已放好：

```bash
test -f "runs/data-v2-abl-d1-p2proto-r2-b16-s42/weights/best.pt"
test -d "/root/yolo_data/images/train"
test -d "/root/yolo_data/images/val"
test -d "/root/yolo_data/labels/train"
test -d "/root/yolo_data/labels/val"
```

如果 D1 权重位于其他长期路径，在后续命令末尾增加 `--model "/absolute/path/best.pt"`，
不要修改并遗留一份未提交的云端 YAML。

## 3. 云端检查与 1 epoch 预检

先让入口补齐仓库运行依赖并解析固定配置；该命令不创建 Run：

```bash
python scripts/cloud_train_data_v2.py \
  --local-refiner \
  --config experiments/data-v2-abl-d1h-localrefine-rb128-s42.yaml \
  --dry-run
```

运行 H 的 CPU/GPU 无关聚焦测试。若镜像没有 pytest，只在云端环境安装一次：

```bash
python -m pip install pytest
python -m pytest ultralytics-main/tests/test_local_refiner.py -q
```

聚焦测试应覆盖：分类器规模与前向、等比例 crop、IoU 标签、概率为 1 时恒等、空候选、
Validator/Predictor 一致，以及组合 checkpoint 回读。

再运行带时间戳的 1 epoch 预检：

```bash
python scripts/cloud_train_data_v2.py \
  --local-refiner \
  --config experiments/data-v2-abl-d1h-localrefine-rb128-s42.yaml \
  --preflight1
```

预检首先复算 D1 Val；Mask mAP50-95 与 `0.37327` 的差异超过 `0.002` 时会停止。随后仅从
Train 生成候选、训练一轮，并走通正式 Val、原图尺寸 Val、机制诊断、GFLOPs 和 batch-1
FP16 测速。预检 Run 不写入 `comparison.csv`，也不创建期刊正式单次记录。

## 4. tmux 正式训练

预检通过后再手动启动唯一正式 Run：

```bash
tmux new -s h-localrefine
```

在 tmux 内执行：

```bash
set -euo pipefail
cd "${HOME}/yolo_plus"
run_id="data-v2-abl-d1h-localrefine-rb128-s42"
log_file="${run_id}.log"
python -u scripts/cloud_train_data_v2.py \
  --local-refiner \
  --config experiments/data-v2-abl-d1h-localrefine-rb128-s42.yaml \
  2>&1 | tee "${log_file}"
```

按 `Ctrl+B`、再按 `D` 退出 tmux。正式 Run 同名目录已存在时入口会停止，不覆盖历史结果。

## 5. 完成检查与打包

正式 Run 至少应包含：

- `weights/best.pt` 与 `weights/last.pt`：D1 与 refiner 的组合 checkpoint；
- `results.csv`、`best_metrics.json`、`original_image_eval.json`；
- `candidate_generation.json`、`mechanism_diagnostics.json`、`gate.json`；
- `complexity.json`：参数量、GFLOPs、batch-1 FP16 延迟和峰值显存；
- `experiment.yaml` 与 `runtime.json`。

核对并打包：

```bash
run_id="data-v2-abl-d1h-localrefine-rb128-s42"
test -f "runs/${run_id}/weights/best.pt"
test -f "runs/${run_id}/results.csv"
test -f "runs/${run_id}/gate.json"
test -f "runs/${run_id}/original_image_eval.json"
test -f "runs/${run_id}/complexity.json"
python scripts/transfer_run.py pack --run-id "${run_id}"
```

将 `exports/data-v2-abl-d1h-localrefine-rb128-s42.zip` 和同名 `.log` 下载回本地。云端不运行
`fill_results_table.py`，不手工填写实验表。

## 6. 固定决策规则

正式 `best.pt` 同时满足以下条件才进入 seed 2、3：

- Val Mask mAP50-95 ≥ `0.37827`；
- 原图尺寸 Val Mask mAP50-95 > `0.34814`；
- 卷叶螟 Mask mAP50-95 ≥ `0.285`；
- 钻心虫 Mask mAP50-95 ≥ `0.456`；
- 原图尺寸中，置信度 ≥ `0.5` 且与全部 GT 框 IoU < `0.1` 的候选比例 < `21.41%`。

H 未通过时不以 D1 单独结题，也不临时放宽门槛；下一步在独立分支实施 I
`Proto26DualP2`。H 通过时再准备 seed 2、3 与 Baseline+H 归因实验，不能直接复用本 Run ID。
