# 实验步骤：参数轮次 mask_ratio=1

> 更新：2026-09-14。F2 已完成并回传；本轮为新的参数优化轮次，源码、配置与云端分支已就绪，待云端预检与正式训练。上一轮 F2 的操作流程与结果见 [F2 正式分析](experiment_records/runs/data-v2-abl-f2-localgate-b16-s42.md)。

## 1. 本次实验

| 项目 | 值 |
|---|---|
| 云端分支 | `feature/data-v2-tune-mr1`（自 `cloud/data-v2-5090` 切出） |
| 代码版本 | 本地提交 `295b3c6`；云端拉取后用 `git log -1 --oneline` 核对，并将完整提交号记入训练日志 |
| 正式 Run ID | `data-v2-tune-mr1-e300-b16-s42` |
| 配置 | `experiments/data-v2-tune-mr1-e300-b16-s42.yaml` |
| 模型 | 官方 `yolo26m-seg.pt`，无结构改动 |
| 相对上轮的唯一改动 | `train.mask_ratio: 2 → 1` |
| 配方 | 300 epoch、patience=100、batch=16、imgsz=640、seed=42、AdamW、AMP |
| 对照 | `data-v2-abl-000-y26m-b16-s42`；Val Mask mAP50-95 = 0.36348、Mask mAP50 = 0.71132、Box mAP50-95 = 0.45629、fitness = 0.81977（best epoch 216） |
| 初始化 | 官方 `yolo26m-seg.pt` |
| 云端数据 / GPU | `/root/yolo_data` / RTX 5090 |

该分支相对基线分支的全部差异是 `experiments/data-v2-tune-mr1-e300-b16-s42.yaml` 一个新增文件，无源码改动。本地已在该分支的独立 worktree 内跑通 `--dry-run`。

## 2. 这一轮只改一个字段的理由

`mask_ratio` 是数据侧参数，决定 GT 多边形栅格化成掩膜目标的倍率（`ultralytics/data/augment.py` 的 `polygons2masks`）。640 输入下掩膜损失在 `imgsz / mask_ratio` 上计算，因此 2 → 1 把掩膜监督的分辨率从 320 提到 640。

本数据集的几何统计（见 F 分支的 `knowledge/data/dataset_geometry_train_val_640.json`）显示：卷叶螟训练集旋转框短边中位数 **7.19 px**，短边小于 8 px 的实例占 **64.82%**；钻心虫同口径为 22.27 px 与 2.65%。栅格化是先降采样再放大，细长目标在粗栅格上先被抹平，放大后无法恢复。

由此给出本轮可证伪的预期：**收益集中在卷叶螟掩膜，钻心虫掩膜基本不动**。若钻心虫掩膜同样大幅上升，说明上述机制解释不成立，本轮结论需改按经验结果记录。

归档中没有干净的 `mask_ratio` 单项对照。第 1 轮参数优化（mr=4、mixup=0.1、lr0=0.01、optimizer=auto，Mask mAP50-95 = 0.35805）与第 2 轮（mr=2、mixup=0、lr0=0.001667、AdamW，0.36348）同时变动四项；`data-v2-scale-y26m-seg-b16-s42`（mr=4）另变动 scale、degrees、flipud 与 epochs。因此本轮的增益幅度没有历史先验，属探索性轮次。

本轮与 `data-v2-abl-000-y26m-b16-s42` 构成干净的单变量对照：同源分支、同一份配方，仅 `mask_ratio` 不同。

## 3. 云端拉取分支

在现有服务器仓库中执行：

```bash
cd /root/yolo_plus
git remote set-branches --add origin feature/data-v2-tune-mr1
git fetch origin
git switch feature/data-v2-tune-mr1
git pull --ff-only origin feature/data-v2-tune-mr1
git log -1 --oneline
git status --short
```

`set-branches --add` 将该分支加入远端抓取范围，也适用于原先只克隆了单个分支的仓库。首次切换会建立本地分支；`git status --short` 应没有输出。

新服务器首次克隆可执行：

```bash
cd /root
git clone --branch feature/data-v2-tune-mr1 https://github.com/xiaojiegenga/yolo_plus.git
cd yolo_plus
```

## 4. 读取训练参数

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-tune-mr1-e300-b16-s42.yaml \
  --run-name data-v2-tune-mr1-e300-b16-s42 --dry-run
```

输出应指向官方 `yolo26m-seg.pt`、`runs/data-v2-tune-mr1-e300-b16-s42/`，且 `mask_ratio: 1`，其余参数与第 1 节一致。`--dry-run` 读取并显示参数，不启动训练。

## 5. 10 epoch 短预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-tune-mr1-e300-b16-s42.yaml \
  --preflight10
```

预检自动使用带 `preflight` 和时间的独立 Run ID。本轮唯一的新增成本是掩膜损失分辨率翻倍（320 → 640），因此预检要重点确认**显存余量**与单 epoch 耗时，并确认训练与 Val 正常完成、损失有限。

0.005 量级的精度差别在 10 epoch 上不可分辨，预检只确认可运行，不用于判断本轮成败。10 epoch 预检不进论文精度排名，不填参数优化表或 `comparison.csv`。正式训练从官方权重重新开始。

## 6. tmux 正式训练

预检通过后创建会话：

```bash
tmux new -s mr1
```

进入 tmux 后执行：

```bash
cd /root/yolo_plus
set -o pipefail
git rev-parse HEAD | tee -a train_data-v2-tune-mr1-e300-b16-s42.log
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-tune-mr1-e300-b16-s42.yaml \
  --run-name data-v2-tune-mr1-e300-b16-s42 2>&1 | tee -a train_data-v2-tune-mr1-e300-b16-s42.log
```

按 `Ctrl+B`，松开后按 `D`，可离开 tmux，训练继续运行。重新进入：

```bash
tmux attach -t mr1
```

正式输出目录为 `runs/data-v2-tune-mr1-e300-b16-s42/`。重跑使用新 Run ID，并同步修改日志名、打包和解包命令中的 ID；训练入口会阻止覆盖已有 Run。

## 7. 打包回传

训练正常完成后，在云端将日志一并收入 Run：

```bash
cp -n train_data-v2-tune-mr1-e300-b16-s42.log runs/data-v2-tune-mr1-e300-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-tune-mr1-e300-b16-s42
```

得到 `exports/data-v2-tune-mr1-e300-b16-s42.zip`。在本地 PowerShell 的项目根目录执行 SCP，SSH 主机和端口填写当前云实例控制台给出的值：

```powershell
$sshHost = Read-Host '云服务器 SSH 主机'
$sshPort = Read-Host '云服务器 SSH 端口'
$runId = 'data-v2-tune-mr1-e300-b16-s42'
New-Item -ItemType Directory -Path '.\exports' -Force | Out-Null
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" `
  '.\exports\'
```

ZIP 下载完成后，在本机同一项目根目录解包：

```powershell
python scripts/transfer_run.py unpack `
  --archive "exports/${runId}.zip"
```

本地已有同名 Run 时不重复解包。完整 Run 保存在 `runs/`，代码仓库只同步轻量记录。

## 8. 结果返回后

按官方 `Box mAP50-95 + Mask mAP50-95` fitness 选择 `best.pt`，比较该权重对应的 Val Mask mAP50-95。

| 对比 | 判断依据 |
|---|---|
| mr1 对 `data-v2-abl-000-y26m-b16-s42` | Val Mask mAP50-95 > 0.36348 判定为本轮配方改进；margin 小于一个 epoch 的波动时不判定 |
| 机制验证 | 分别报出卷叶螟与钻心虫的 Mask mAP50-95 及 mask P/R，检验第 2 节的预期方向 |

本轮的记录分路：属参数优化，分析写到 `experiment_records/parameter_tuning/<run-id>.md`，性能只填《云服务器实验设计与记录表》表 2；**不运行 `fill_results_table.py`，不进 `comparison.csv`，不进表 1**。

若本轮胜出，配方即发生变更，而现有 8 个结构消融均在 mr=2 下完成，表 1 的对照基准随之改变。此时需先在这两条路中定一条，再动任何结构 Run：

- 重跑 baseline 与全部结构模块（口径统一，代价为每个模块一次正式训练）；
- 表 1 保留 mr=2 作探索记录，仅在最终论文口径下重跑 baseline 与最终选定的模块。

Test 留到最终方案冻结后统一评估。
