# 实验步骤：改进 A2（P3 ZR-CBAM）

> 用途：在 RTX 5090 云服务器上完成改进 A2 的 10 epoch 预检与 300 epoch 正式训练。
> 本文件只给出操作顺序和命令；正式训练由用户在云服务器手动启动。

## 0. 本次实验身份

| 项目 | 固定值 |
|---|---|
| 正式 Baseline | `data-v2-abl-000-y26m-b16-s42` |
| A1 参照 | `data-v2-abl-100-srcbam-b16-s42`；正式结果已提交为 `ac11686` |
| 当前分支 | `feature/data-v2-abl-a-attention` |
| A2 正式 Run ID | `data-v2-abl-a2-p3-zrcbam-b16-s42` |
| A2 预检 Run ID | `data-v2-abl-a2-p3-zrcbam-b16-s42-preflight10` |
| 模型结构 | YOLO26m-seg + Backbone P3 ZR-CBAM |
| 模型 YAML | `ultralytics-main/ultralytics/cfg/models/26/yolo26m-p3-zrcbam-seg.yaml` |
| 实验配置 | `experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml` |
| 预训练权重 | 官方 `yolo26m-seg.pt` |
| 数据 | `rice-pest-data-v2`，云端 `/root/yolo_data` |
| 训练参数 | epochs=300，batch=16，imgsz=640，seed=42，其余与正式 `000` 相同 |
| 唯一结构变量 | Backbone P3/8 的零初始化加法残差 CBAM |

A2 使用：

```text
X_att = CBAM(X)
Y = X + beta × X_att
beta 初始值 = 0
```

初始输出与 Baseline 完全一致，训练期间由 `beta` 学习注意力残差的方向和强度。A2 只在
Backbone 第 4 层 P3/8 插入模块，第 6 层 P4/16 恢复为普通 `C3k2`。

## 1. 云端拉取 A2 提交

已有 `/root/yolo_plus` 仓库时执行：

```bash
cd /root/yolo_plus
git fetch origin
git switch feature/data-v2-abl-a-attention
git pull --ff-only
git branch --show-current
git log -1 --oneline
git status --short
```

如果本机还没有该本地分支，则执行：

```bash
cd /root/yolo_plus
git fetch origin
git switch --track origin/feature/data-v2-abl-a-attention
git log -1 --oneline
git status --short
```

首次克隆时执行：

```bash
cd /root
git clone --branch feature/data-v2-abl-a-attention https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd /root/yolo_plus
git log -1 --oneline
```

`git status --short` 应无输出。`git log -1` 应显示本次 A2 提交，而不是 A1 的
`ac11686`。

## 2. 检查配置、模型和数据

```bash
cd /root/yolo_plus
ls experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml
ls ultralytics-main/ultralytics/cfg/models/26/yolo26m-p3-zrcbam-seg.yaml
ls /root/yolo_data/images/train
ls /root/yolo_data/images/val
ls /root/yolo_data/labels/train
ls /root/yolo_data/labels/val
```

本次不修改 Test 数据，也不用 Test 选择方案。

## 3. Dry-run 检查最终参数

```bash
python scripts/train_yolo26_seg.py \
  --config experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml \
  --run-name data-v2-abl-a2-p3-zrcbam-b16-s42 \
  --dry-run
```

重点核对：

- `[MODEL]` 指向 `yolo26m-p3-zrcbam-seg.yaml`；
- `[PRETRAINED]` 为 `yolo26m-seg.pt`；
- epochs=300、batch=16、imgsz=640、seed=42；
- mask_ratio=2、mixup=0、optimizer=AdamW；
- 输出目录为 `runs/data-v2-abl-a2-p3-zrcbam-b16-s42`。

Dry-run 不导入模型、不下载权重、也不启动训练。

## 4. 运行 10 epoch 预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml \
  --run-name data-v2-abl-a2-p3-zrcbam-b16-s42-preflight10 \
  --preflight10
```

预检中确认：

- 模型第 4 层为 `C3k2ZRCBAM`，第 6 层仍为普通 `C3k2`；
- 官方 `yolo26m-seg.pt` 权重能够迁移，自定义注意力参数为新增参数；
- 训练与 Val 能完成，loss 和 mAP 没有持续 NaN/Inf；
- batch=16 不发生 OOM；
- 输出目录为 `runs/data-v2-abl-a2-p3-zrcbam-b16-s42-preflight10`。

预检只检查流程，不进入 `comparison.csv`、实验总表或论文精度排名。正式 Run ID 与预检
Run ID 不同，因此无需删除预检结果。

## 5. 手动启动 300 epoch 正式训练

预检正常后，在云端执行：

```bash
cd /root/yolo_plus
nohup python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml \
  --run-name data-v2-abl-a2-p3-zrcbam-b16-s42 \
  > train_data-v2-abl-a2-p3-zrcbam-b16-s42.log 2>&1 &
```

查看日志：

```bash
tail -f train_data-v2-abl-a2-p3-zrcbam-b16-s42.log
```

退出日志查看但不中止训练：按 `Ctrl+C`。检查进程和显存：

```bash
ps -ef | grep '[c]loud_train_data_v2.py'
watch -n 3 nvidia-smi
```

正式输出目录必须是：

```text
runs/data-v2-abl-a2-p3-zrcbam-b16-s42/
```

若同名目录已经存在，训练入口会停止。不要覆盖旧 Run；重跑必须另取 Run ID。

## 6. 训练完成后检查并打包

```bash
ls runs/data-v2-abl-a2-p3-zrcbam-b16-s42/args.yaml
ls runs/data-v2-abl-a2-p3-zrcbam-b16-s42/results.csv
ls runs/data-v2-abl-a2-p3-zrcbam-b16-s42/weights/best.pt
ls runs/data-v2-abl-a2-p3-zrcbam-b16-s42/weights/last.pt
```

打包完整 Run：

```bash
python scripts/transfer_run.py pack \
  --run-id data-v2-abl-a2-p3-zrcbam-b16-s42
```

预期归档：

```text
exports/data-v2-abl-a2-p3-zrcbam-b16-s42.zip
```

## 7. Windows 本地 SCP 下载

在本地 PowerShell 设置当前云实例的地址和 SSH 端口：

```powershell
$sshHost = "替换为当前云服务器地址"
$sshPort = 12345
$runId = "data-v2-abl-a2-p3-zrcbam-b16-s42"
$projectRoot = "E:\study\graduate_sec\论文撰写\模型训练"
```

下载 ZIP：

```powershell
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" `
  "${projectRoot}\exports\"
```

在本地项目根目录解包：

```powershell
python scripts/transfer_run.py unpack `
  --archive "exports/${runId}.zip"
```

下载 `nohup` 训练日志到对应 Run：

```powershell
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/train_${runId}.log" `
  "${projectRoot}\runs\${runId}\"
```

## 8. 本地正式结果登记

完整 Run 和训练日志到达本地后执行：

```powershell
python scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-a2-p3-zrcbam-b16-s42" `
  --run-id "data-v2-abl-a2-p3-zrcbam-b16-s42" `
  --data data-v2
```

随后完成：

1. 创建 `experiment_records/runs/data-v2-abl-a2-p3-zrcbam-b16-s42.md`；
2. 读取 `args.yaml`、`results.csv`、`best.pt` 和训练日志；
3. 填写总表中 A2 的总体、分类别和资源结果；
4. 与正式 `000` 和 A1 分别比较 Mask mAP50-95、Mask mAP50、分类别指标与成本；
5. 根据 A2 正式结果决定注意力改进 A 是保留、继续小改，还是终止。

## 9. 固定边界

- A2 只使用 Val 选择权重和方案；
- Test 保留到最终方案与推理阈值冻结后统一评估；
- 正式训练参数与 `000` 完全相同，不在云端临时修改 batch、学习率或增强；
- A2 的唯一结构变量是 P3 ZR-CBAM；
- 预检不进入正式实验记录；
- 不覆盖现有 Run、ZIP、权重或历史记录；
- `runs/`、`exports/`、数据和权重不提交 GitHub。
