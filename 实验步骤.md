# 实验步骤：改进 B（实例分割 Dice Loss）

> 用途：A2 训练结束并完成打包后，在 RTX 5090 云服务器上运行改进 B。本文只规定操作顺序和命令，正式训练由用户手动启动。

## 0. 本次实验身份

| 项目 | 固定值 |
|---|---|
| 对照 Baseline | `data-v2-abl-000-y26m-b16-s42` |
| Git 分支 | `feature/data-v2-abl-b-dice` |
| 正式 Run ID | `data-v2-abl-010-dice-b16-s42` |
| 10 epoch 预检 Run ID | `data-v2-abl-010-dice-b16-s42-preflight10` |
| 模型起点 | 官方 `yolo26m-seg.pt` |
| 数据集 | `rice-pest-data-v2`，云端目录 `/root/yolo_data` |
| 正式配置 | `experiments/data-v2-abl-010-dice-b16-s42.yaml` |
| 唯一改进 | 实例掩膜损失由原 BCE 改为 `BCE + 0.5 × Dice` |
| Dice 平滑项 | `1.0` |
| 训练 | epochs=300，patience=100，batch=16，nbs=64 |
| 输入与随机性 | imgsz=640，seed=42，deterministic=true |
| 优化器 | AdamW，lr0=0.001667，lrf=0.01，momentum=0.9，weight_decay=0.0005 |
| 掩膜与增强 | mask_ratio=2，mixup=0，mosaic=1，copy_paste=0.3 |
| 主指标 | Val Mask mAP50-95 |

对每个匹配到的实例，在目标框区域内计算：

$$
L_{Dice}=1-\frac{2\sum_i p_i g_i+1}{\sum_i p_i+\sum_i g_i+1}
$$

$$
L_{mask}=L_{BCE}+0.5L_{Dice}
$$

其中 $p_i=\operatorname{sigmoid}(z_i)$，$z_i$ 是预测掩膜 logit，$g_i$ 是真实二值掩膜。B 分支不包含 A1/A2 注意力模块，也不改变模型结构、推理流程和参数量。

## 1. 等待 A2 完整结束

先确认 A2 的训练进程已经结束，并且已经完成以下工作：

- A2 Run 中存在 `weights/best.pt`、`results.csv` 和 `args.yaml`；
- A2 Run 已用 `scripts/transfer_run.py pack` 打包；
- A2 ZIP 已回传本地或确认可以稍后下载。

不要在 A2 仍训练或正在打包时切换仓库分支。

## 2. 云端切换并更新 B 分支

已有 `/root/yolo_plus` 仓库时执行：

```bash
cd /root/yolo_plus
git fetch origin
git status --short
git switch feature/data-v2-abl-b-dice
git pull --ff-only
git branch --show-current
git log --oneline -1
git status --short
```

检查结果：

- 当前分支必须是 `feature/data-v2-abl-b-dice`；
- `git status --short` 应无输出；
- 最新提交应包含改进 B 的源码、配置和实验步骤。

如果云端还没有本地 B 分支，改用：

```bash
git switch --track origin/feature/data-v2-abl-b-dice
```

如果需要重新克隆：

```bash
cd /root
git clone --branch feature/data-v2-abl-b-dice https://github.com/xiaojiegenga/yolo_plus.git
cd /root/yolo_plus
```

## 3. 确认数据和 B 配置

```bash
ls experiments/data-v2-abl-010-dice-b16-s42.yaml
ls experiments/yolo_data_v2_cloud.yaml
ls /root/yolo_data/images/train
ls /root/yolo_data/labels/train
```

本阶段不重新划分数据，不修改标签，也不使用 Test 调参。

## 4. 打印配置，不启动训练

```bash
python scripts/train_yolo26_seg.py \
  --config experiments/data-v2-abl-010-dice-b16-s42.yaml \
  --run-name data-v2-abl-010-dice-b16-s42 \
  --dry-run
```

重点核对：

```text
model=yolo26m-seg.pt
epochs=300, patience=100, batch=16, nbs=64, imgsz=640
optimizer=AdamW, lr0=0.001667, lrf=0.01
mask_ratio=2, seed=42, deterministic=true, split=val
instance_dice_gain=0.5, instance_dice_smooth=1.0
```

若上述值不一致，不要在云端手工改 YAML，应先在本地修正、提交和推送，再让云端重新拉取。

## 5. 运行 10 epoch 预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-010-dice-b16-s42.yaml \
  --run-name data-v2-abl-010-dice-b16-s42-preflight10 \
  --preflight10
```

预检通过条件：

- 正确加载官方 `yolo26m-seg.pt` 和 data-v2；
- 训练参数中显示 `instance_dice_gain=0.5` 和 `instance_dice_smooth=1.0`；
- RTX 5090 上 batch=16 不发生 OOM；
- 训练、反向传播、Val 和权重保存正常完成；
- loss 与 Box/Mask 指标均为有限值，没有 NaN 或 Inf。

预检只用于确认代码和流程可运行，不进入 `comparison.csv`、消融表或单次正式实验分析。若预检失败，保留日志并停止，不启动 300 epoch。

## 6. 启动 B 正式训练

推荐后台运行：

```bash
nohup python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-010-dice-b16-s42.yaml \
  --run-name data-v2-abl-010-dice-b16-s42 \
  > train_data-v2-abl-010-dice-b16-s42.log 2>&1 &
```

查看日志：

```bash
tail -f train_data-v2-abl-010-dice-b16-s42.log
```

查看 GPU：

```bash
nvidia-smi
```

正式结果目录必须是：

```text
runs/data-v2-abl-010-dice-b16-s42/
```

由于 B 的 `seg_loss` 已包含 Dice 项，其数值尺度可能高于 Baseline 的纯 BCE `seg_loss`，不能仅凭两条 loss 的绝对大小判断 B 是否更好。方案比较以同一 Val 上的 Mask mAP50-95 为主。

## 7. 训练完成后打包

确认进程已经结束，并且 Run 内存在 `weights/best.pt`、`results.csv` 和 `args.yaml` 后执行：

```bash
python scripts/transfer_run.py pack \
  --run-id data-v2-abl-010-dice-b16-s42
```

预期生成：

```text
exports/data-v2-abl-010-dice-b16-s42.zip
```

## 8. 用 SCP 回传本地

在 Windows 本地 PowerShell 中，将端口和主机名替换为当前云服务器实例提供的值：

```powershell
scp -P <SCP_PORT> root@<CLOUD_HOST>:/root/yolo_plus/exports/data-v2-abl-010-dice-b16-s42.zip "E:\study\graduate_sec\论文撰写\模型训练\exports\"
scp -P <SCP_PORT> root@<CLOUD_HOST>:/root/yolo_plus/train_data-v2-abl-010-dice-b16-s42.log "E:\study\graduate_sec\论文撰写\模型训练\runs\data-v2-abl-010-dice-b16-s42\"
```

在本地项目根目录解包：

```powershell
python scripts/transfer_run.py unpack --archive "exports/data-v2-abl-010-dice-b16-s42.zip"
```

## 9. 本地正式结果登记

完整 Run 解包到 `runs/data-v2-abl-010-dice-b16-s42/` 后：

```powershell
python scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-010-dice-b16-s42" `
  --run-id "data-v2-abl-010-dice-b16-s42" `
  --data data-v2
```

随后完成：

1. 创建或更新 `experiment_records/runs/data-v2-abl-010-dice-b16-s42.md`；
2. 记录 best epoch、训练时间、显存、Box/Mask 总体和分类别指标；
3. 更新 `experiment_records/comparison.csv`；
4. 将 B 的正式结果填入 `云服务器实验设计与记录表.md` 对应表格；
5. 用 Val Mask mAP50-95 与 Baseline、A1、A2 比较，不提前使用 Test。

## 10. 实验边界

- B 分支只改实例掩膜损失，不含注意力模块和 P2Head；
- B 与 Baseline 使用同一数据、预训练权重、参数、seed 和 Val 口径；
- 不覆盖同名 Run；如果确需重跑，使用新的 Run ID；
- Test 仅在最终方案和推理设置全部冻结后统一评估。
