# 实验步骤：改进 E（Neck DySample）

> 更新：2026-09-09。E 已完成 300 epoch 并回传分析，本次未通过门控，不进入 D1+E。以下保留本次操作命令供追溯，同名正式 Run 已使用。结果见 [E 正式分析](experiment_records/runs/data-v2-abl-e-dysample-b16-s42.md)。

## 1. 本次实验

| 项目 | 值 |
|---|---|
| 云端分支 | `feature/data-v2-abl-e-dysample` |
| 基点 / E 实现提交 | 正式 Baseline `c0f4f35` / `5d91a2b` |
| 正式 Run ID | `data-v2-abl-e-dysample-b16-s42` |
| 配置 | `experiments/data-v2-abl-e-dysample-b16-s42.yaml` |
| 模型 | `yolo26m-dysample-seg.yaml`；第 11、14 层 DySample |
| 配方 | 与 000 相同；300 epoch、batch=16、imgsz=640、seed=42、AMP |
| 对照 | `data-v2-abl-000-y26m-b16-s42` |
| 初始化 | 官方 `yolo26m-seg.pt` |

先阅读 [E 简明原理说明](knowledge/改进E-DySample动态上采样原理与实现.md)。本轮验证 Baseline+E，D1+E 留待 E 结果返回后决定。
D1 结果与源码快照已保存在总体分析分支 `631a932`；D1 独立源码分支仍保留。

## 2. 云端拉取 E

在现有服务器仓库中执行：

```bash
cd /root/yolo_plus
git fetch origin
git switch feature/data-v2-abl-e-dysample
git pull --ff-only
git log -1 --oneline
git status --short
```

首次切换时 Git 会根据远端分支建立本地跟踪。`git status --short` 应没有输出。

## 3. 读取参数并检查结构

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-e-dysample-b16-s42.yaml \
  --run-name data-v2-abl-e-dysample-b16-s42 --dry-run
python scripts/check_dysample.py --weights yolo26m-seg.pt --cuda
```

检查只使用合成输入，无需 pytest。预期 7 项 CPU 测试通过，随后官方权重检查和 CUDA AMP 检查通过。
迁移日志为 904/910（80 类模型首次加载）、896/910（重建为 2 类，包含新增参数复制）；
最终直接来自官方权重的张量为 890/910，其余包括类别尺寸变化和新增上采样张量。

若出现 `grid_sampler_2d_backward_cuda ... deterministic` 警告，表示 CUDA 反向不保证逐位复现。
框架使用 `warn_only=True`，警告不等于训练失败，保留冻结的 deterministic 参数即可。

## 4. 10 epoch 短预检

建议先确认云端新算子的训练、Val、AMP 和显存：

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-e-dysample-b16-s42.yaml \
  --run-name data-v2-abl-e-dysample-preflight10-b16-s42 --preflight10
```

预检使用独立 Run ID，不进入论文结果表。正式实验从官方权重重新开始。

## 5. tmux 正式训练

确认云端检查通过后，由用户手动执行：

```bash
tmux new -s e-dysample
```

进入 tmux 后：

```bash
cd /root/yolo_plus
set -o pipefail
git rev-parse HEAD > train_data-v2-abl-e-dysample-b16-s42.log
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-e-dysample-b16-s42.yaml \
  --run-name data-v2-abl-e-dysample-b16-s42 2>&1 | tee -a train_data-v2-abl-e-dysample-b16-s42.log
```

按 `Ctrl+B`，松开后按 `D`，可离开 tmux，训练继续运行。重新进入：

```bash
tmux attach -t e-dysample
```

正式 Run ID 已存在时使用新 ID，训练入口会阻止覆盖已有 Run。

## 6. 打包回传

训练正常完成后，在云端将日志一并收入 Run：

```bash
cp -n train_data-v2-abl-e-dysample-b16-s42.log runs/data-v2-abl-e-dysample-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-e-dysample-b16-s42
```

得到 `exports/data-v2-abl-e-dysample-b16-s42.zip`。通过已有文件传输方式下载到本地 `exports/`，然后在本地执行：

```powershell
& D:/tool/Anaconda3/envs/yolo26/python.exe scripts/transfer_run.py unpack `
  --archive exports/data-v2-abl-e-dysample-b16-s42.zip
```

本地已有同名 Run 时无需再次解包。完整 Run 保存在 `runs/`，代码仓库只同步轻量记录。

## 7. 结果返回后

按 official fitness 选择 best.pt 对应轮次；主指标比较 Val Mask mAP50-95，同时报告 Mask mAP50。
E 单独结果与 Baseline 比较，再决定是否准备 D1+E。正式记录由本地回填 `comparison.csv` 和实验总表。
当前 [两结构消融计划](experiment_records/data-v2-de-ablation-plan.md) 已固定，Test 留到最终方案冻结后使用。
