# 实验步骤：改进 A（SR-CBAM）

> 用途：在 RTX 5090 云服务器上完成 data-v2 正式消融 `100`。
> 本文件只给出操作顺序和命令；训练由用户在云服务器手动启动。

## 0. 本次实验身份

| 项目 | 固定值 |
|---|---|
| 正式 Baseline | `data-v2-abl-000-y26m-b16-s42` |
| Baseline 分支 / 记录提交 | `cloud/data-v2-5090` / `c0f4f35` |
| 改进 A 分支 | `feature/data-v2-abl-a-attention` |
| 正式 Run ID | `data-v2-abl-100-srcbam-b16-s42` |
| 10 epoch 预检 Run ID | `data-v2-abl-100-srcbam-b16-s42-preflight10` |
| 模型结构 | YOLO26m-seg + Backbone P3/P4 SR-CBAM |
| 模型 YAML | `ultralytics-main/ultralytics/cfg/models/26/yolo26m-srcbam-seg.yaml` |
| 实验配置 | `experiments/data-v2-abl-100-srcbam-b16-s42.yaml` |
| 预训练权重 | 官方 `yolo26m-seg.pt` |
| 数据 | `rice-pest-data-v2`，云端 `/root/yolo_data` |
| 训练参数 | epochs=300，patience=100，batch=16，nbs=64，imgsz=640 |
| 随机性 | seed=42，deterministic=true |
| 对照原则 | 相对 `000` 只改变 P3/P4 的 SR-CBAM |

SR-CBAM 的原理、公式和源码位置见：

```text
knowledge/改进A-SR-CBAM注意力机制原理与实现.md
```

## 1. 云服务器拉取改进 A 分支

进入已有仓库：

```bash
cd /root/yolo_plus
git fetch origin
git switch --track origin/feature/data-v2-abl-a-attention
git pull --ff-only
git branch --show-current
git log --oneline -2
```

这是云端第一次切换到该功能分支时使用的命令。如果本地分支已经存在，则改用：

```bash
cd /root/yolo_plus
git fetch origin
git switch feature/data-v2-abl-a-attention
git pull --ff-only
```

当前分支必须显示：

```text
feature/data-v2-abl-a-attention
```

确认工作区没有云端临时改动：

```bash
git status --short
```

正常情况下没有输出。

## 2. 确认配置、源码和数据目录

```bash
ls experiments/data-v2-abl-100-srcbam-b16-s42.yaml
ls ultralytics-main/ultralytics/cfg/models/26/yolo26m-srcbam-seg.yaml
ls /root/yolo_data/images/train
ls /root/yolo_data/labels/train
```

本次不修改云端 YAML，不重新划分数据，也不使用 Test。

## 3. dry-run：只打印参数

```bash
python scripts/train_yolo26_seg.py \
  --config experiments/data-v2-abl-100-srcbam-b16-s42.yaml \
  --run-name "data-v2-abl-100-srcbam-b16-s42" \
  --dry-run
```

重点核对：

```text
[MODEL] .../yolo26m-srcbam-seg.yaml
[PRETRAINED] yolo26m-seg.pt
epochs: 300
batch: 16
nbs: 64
imgsz: 640
optimizer: AdamW
mask_ratio: 2
mixup: 0.0
seed: 42
split: val
```

dry-run 不会创建 Run，也不会启动训练。

## 4. 用户手动运行 10 epoch 预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-100-srcbam-b16-s42.yaml \
  --run-name "data-v2-abl-100-srcbam-b16-s42-preflight10" \
  --preflight10
```

第一次使用时，Ultralytics 可能下载 `yolo26m-seg.pt`。预检日志需要确认：

- 模型表中 Backbone 第 4、6 层为 `C3k2SRCBAM`；
- 出现从 `yolo26m-seg.pt` 迁移权重的 `Transferred ... items` 提示；
- GPU 为 RTX 5090，batch=16 没有 OOM；
- 完成 10 个 epoch、Val 和权重保存；
- 训练 loss 与 Box/Mask AP 没有持续 NaN/Inf；
- 结果目录为 `runs/data-v2-abl-100-srcbam-b16-s42-preflight10/`。

预检只检查流程，不进入总表、正式 Run 记录或 `comparison.csv`。预检失败时先保留终端
错误和日志，不要启动正式训练。

## 5. 用户手动启动 300 epoch 正式训练

预检通过后使用后台运行，避免 SSH 断开中止训练：

```bash
cd /root/yolo_plus
nohup python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-100-srcbam-b16-s42.yaml \
  --run-name "data-v2-abl-100-srcbam-b16-s42" \
  > train_data-v2-abl-100-srcbam-b16-s42.log 2>&1 &
```

查看训练日志：

```bash
tail -f train_data-v2-abl-100-srcbam-b16-s42.log
```

退出日志查看但不停止训练：

```text
Ctrl+C
```

查看 GPU：

```bash
watch -n 3 nvidia-smi
```

确认训练进程：

```bash
ps -ef | grep '[c]loud_train_data_v2.py'
```

正式输出目录必须是：

```text
runs/data-v2-abl-100-srcbam-b16-s42/
```

若同名目录已经存在，训练入口会停止。不要删除或覆盖旧 Run；需要重跑时先确定新的 Run ID。

## 6. 训练完成后检查并打包

确认日志已经显示训练结束，然后检查核心文件：

```bash
ls runs/data-v2-abl-100-srcbam-b16-s42/args.yaml
ls runs/data-v2-abl-100-srcbam-b16-s42/results.csv
ls runs/data-v2-abl-100-srcbam-b16-s42/weights/best.pt
ls runs/data-v2-abl-100-srcbam-b16-s42/weights/last.pt
```

打包完整 Run：

```bash
python scripts/transfer_run.py pack \
  --run-id "data-v2-abl-100-srcbam-b16-s42"
```

预期归档：

```text
exports/data-v2-abl-100-srcbam-b16-s42.zip
```

## 7. Windows 本地 SCP 下载

在本地 PowerShell 设置云实例当前显示的 SSH 主机和端口：

```powershell
$sshHost = "替换为当前云服务器地址"
$sshPort = 12345
$runId = "data-v2-abl-100-srcbam-b16-s42"
$projectRoot = "E:\study\graduate_sec\论文撰写\模型训练"
```

下载 ZIP：

```powershell
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" `
  "${projectRoot}\exports\"
```

在本地项目根目录解包：

```powershell
python scripts/transfer_run.py unpack `
  --archive "exports/${runId}.zip"
```

解包完成后，再下载 `nohup` 训练日志到对应 Run：

```powershell
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/train_${runId}.log" `
  "${projectRoot}\runs\${runId}\"
```

## 8. 本地正式结果登记

完整 Run 和训练日志到达本地后执行：

```powershell
python scripts/fill_results_table.py `
  --run-dir "runs/data-v2-abl-100-srcbam-b16-s42" `
  --run-id "data-v2-abl-100-srcbam-b16-s42" `
  --data data-v2
```

随后完成：

1. 从 `experiment_records/runs/_template.md` 创建
   `experiment_records/runs/data-v2-abl-100-srcbam-b16-s42.md`；
2. 读取 `args.yaml`、`results.csv`、`best.pt` 验证输出和训练日志；
3. 填写总表表 4、8、9、10 和 `comparison.csv`；
4. 与 `data-v2-abl-000-y26m-b16-s42` 比较 Mask mAP50-95、Mask mAP50、分类别指标和资源开销；
5. 根据正式结果决定 A 是否进入组合实验。

## 9. 固定边界

- 本次只使用 Val 选择权重和方案；
- Test 保留到最终方案与推理阈值冻结后统一评估；
- 正式训练参数与 `000` 完全相同，不在云端临时调 batch、学习率或增强；
- 预检不进入论文精度排名；
- 不覆盖现有 Run、ZIP、权重或记录；
- `runs/`、`exports/`、数据和权重不提交 GitHub。
