# 实验步骤：结构消融 G（P3 定向条带上下文）

> 更新：2026-09-15。**G 已完成正式训练并回传，结论为未通过门控**：Val Mask mAP50-95=0.36020，较 000 低 0.00328，差额小于单轮波动中位数，与 Baseline 不可区分；完整分析见 [G 正式记录](experiment_records/runs/data-v2-abl-g-oristrip-b16-s42.md)。本页保留本轮的拉取、预检、训练、回传与判定流程，供同类结构消融复用。本轮是**结构消融，进表 1**，不是参数轮次。上一轮 mask_ratio=1 的运行单已由 Git 历史归档；F2 已有回传 Run（`runs/data-v2-abl-f2-localgate-b16-s42/`），其正式分析记录尚未建立。

## 1. 本次实验

| 项目 | 值 |
|---|---|
| 云端分支 | `feature/data-v2-abl-g-oristrip`（自基线分支 `cloud/data-v2-5090` 切出） |
| 代码版本 | 本地提交 `73ec6c0`；云端拉取后用 `git log -1 --oneline` 核对，并将完整提交号记入训练日志 |
| 正式 Run ID | `data-v2-abl-g-oristrip-b16-s42` |
| 配置 | `experiments/data-v2-abl-g-oristrip-b16-s42.yaml` |
| 模型 | `ultralytics-main/ultralytics/cfg/models/26/yolo26m-p3-oristrip-seg.yaml` |
| 相对基线的唯一改动 | Neck 第 16 层 `C3k2 [256, True]` → `C3k2OrientedStrip [256, True]` |
| 初始化 | 官方 `yolo26m-seg.pt`，由配置顶层 `pretrained` 迁移 |
| 配方 | 与 `data-v2-abl-000-y26m-b16-s42` 逐项相同：300 epoch、patience=100、batch=16、imgsz=640、seed=42、AdamW、AMP、mask_ratio=2 |
| 对照 | `data-v2-abl-000-y26m-b16-s42`；Val Mask mAP50-95 = 0.36348、Mask mAP50 = 0.71132、Box mAP50-95 = 0.45629、fitness = 0.81977（best epoch 216） |
| 云端数据 / GPU | `/root/yolo_data` / RTX 5090 |

该分支相对基线分支的全部差异是：模块源码 `ultralytics/nn/modules/block.py`、导出 `modules/__init__.py`、`parse_model` 的注册 `nn/tasks.py`，新增的模型 YAML、训练配置与聚焦测试三个文件，另加训练入口脚本读取配置顶层 `pretrained` 的一小段。模型的层号、层数、`Segment26` 的输入层 `[16, 19, 22]` 与基线一致，改进是单变量。

## 2. 结构设计与只改一处的理由

**目标几何。** 卷叶螟是细长目标：训练集旋转框短边中位数 **7.17 px**、长边中位数 **81.48 px**、长宽比中位数 **11.02**，短边小于 8 px 的实例占 **65.28%**；钻心虫同口径为 22.27 px、91.01 px、3.85（`knowledge/data/dataset_geometry_train_val_640.json`）。P3 步长是 8，要看住 81.48 px 的长边，条带需要 11 个抽头（88 px）；F 的 7 抽头只有 56 px，够不到。

**模块。** `OrientedStripContext` 接在第 16 层输出上：1×1 Conv-BN-SiLU 把 256 通道压到 64，五条深度可分离分支并行——3×3 局部、1×11 水平、11×1 垂直、11×11 主对角与 11×11 副对角——拼成 320 通道，再由 1×1 线性投影回 256 加在输入上。两条对角分支用固定掩码只保留自己那条对角线上的 11 个抽头，代价是每个输出一个抽头。

**两个方向都留分支的理由。** 无人机俯拍下卷叶螟朝向任意，只给横纵条带会把斜向的实例漏掉；对角分支补上这一半，模块对方向的覆盖才是完整的。

**零初始化投影。** 投影权重与偏置初始化为 0，模块起点是恒等映射，基线权重迁移进来后前向输出与基线逐位相同，训练也不会因为新增分支的随机初始化而被扰动。这一点有测试守着（`torch.equal(module(inputs), inputs)`，以及恒等状态下候选模型与源模型输出逐位一致）。

**成本。** 参数量 26,971,750 → 27,088,550，**+116,800（+0.433%）**；GFLOPs 131.9 → 133.2（**+0.99%**）。新增参数里 82,176 落在那个 1×1 投影上，五条条带分支合计只有 18,112。代价这么小，收益不会被"靠参数量堆出来"解释掉。

**可证伪的预期。** 收益应集中在卷叶螟的框与掩膜，钻心虫（短边中位数 22.27 px）基本不动。若两类同幅上升，说明条带几何这套解释不成立，本轮结论按经验结果记录。

`mask_ratio` 保持 2 不变，因此本轮与表 1 的基线对照组口径一致，表 1 的基准不需要改动。

## 3. 云端拉取分支

在现有服务器仓库中执行：

```bash
cd /root/yolo_plus
git remote set-branches --add origin feature/data-v2-abl-g-oristrip
git fetch origin
git switch feature/data-v2-abl-g-oristrip
git pull --ff-only origin feature/data-v2-abl-g-oristrip
git log -1 --oneline
git status --short
```

`set-branches --add` 将该分支加入远端抓取范围，也适用于原先只克隆了单个分支的仓库。首次切换会建立本地分支；`git log -1 --oneline` 应显示 `73ec6c0 feat: add P3 oriented strip-context ablation G`，`git status --short` 应没有输出。

新服务器首次克隆可执行：

```bash
cd /root
git clone --branch feature/data-v2-abl-g-oristrip https://github.com/xiaojiegenga/yolo_plus.git
cd yolo_plus
```

## 4. 读取训练参数

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-g-oristrip-b16-s42.yaml \
  --run-name data-v2-abl-g-oristrip-b16-s42 --dry-run
```

输出应是：

```text
[MODEL] .../ultralytics-main/ultralytics/cfg/models/26/yolo26m-p3-oristrip-seg.yaml
[PRETRAINED] yolo26m-seg.pt
[OUTPUT] .../runs/data-v2-abl-g-oristrip-b16-s42
```

`[PRETRAINED]` 这一行是本轮的检查点：自定义结构 YAML 靠配置顶层的 `pretrained` 迁移官方权重，基线分支的入口脚本不读这个字段，G 分支才补上。**若这一行缺失，说明拉到的代码版本不对，不要继续训练**——那会变成从随机初始化重训，与基线不可比。训练参数里 `mask_ratio: 2`，其余与第 1 节一致。`--dry-run` 只读参数，不启动训练。

## 5. 10 epoch 短预检

```bash
python scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-g-oristrip-b16-s42.yaml \
  --preflight10
```

预检自动使用带 `preflight` 和时间的独立 Run ID。本轮要确认两点：

- **结构解析正确**：日志里模型的层数应为 342（nc=2、scale m 的本地构建值），基线是 329。层数一致说明 `C3k2OrientedStrip` 没被当成未知模块跳过。
- **显存与单 epoch 耗时**：新增的是五条深度可分离分支与一个 320→256 的 1×1 投影，P3 分辨率下开销不大，但要确认显存余量与基线在同一档。

0.005 量级的精度差别在 10 epoch 上不可分辨，预检只确认可运行，不用于判断本轮成败。10 epoch 预检不进论文精度排名，不进表 1、表 2 或 `comparison.csv`。正式训练从官方权重重新开始。

## 6. tmux 正式训练

预检通过后创建会话：

```bash
tmux new -s oristrip
```

进入 tmux 后执行：

```bash
cd /root/yolo_plus
set -o pipefail
git rev-parse HEAD | tee -a train_data-v2-abl-g-oristrip-b16-s42.log
python -u scripts/cloud_train_data_v2.py \
  --config experiments/data-v2-abl-g-oristrip-b16-s42.yaml \
  --run-name data-v2-abl-g-oristrip-b16-s42 2>&1 | tee -a train_data-v2-abl-g-oristrip-b16-s42.log
```

按 `Ctrl+B`，松开后按 `D`，可离开 tmux，训练继续运行。重新进入：

```bash
tmux attach -t oristrip
```

正式输出目录为 `runs/data-v2-abl-g-oristrip-b16-s42/`。重跑使用新 Run ID，并同步修改日志名、打包和解包命令中的 ID；训练入口会阻止覆盖已有 Run。

## 7. 打包回传

训练正常完成后，在云端将日志一并收入 Run：

```bash
cp -n train_data-v2-abl-g-oristrip-b16-s42.log runs/data-v2-abl-g-oristrip-b16-s42/
python scripts/transfer_run.py pack --run-id data-v2-abl-g-oristrip-b16-s42
```

得到 `exports/data-v2-abl-g-oristrip-b16-s42.zip`。在本地 PowerShell 的项目根目录执行 SCP，SSH 主机和端口填写当前云实例控制台给出的值：

```powershell
$sshHost = Read-Host '云服务器 SSH 主机'
$sshPort = Read-Host '云服务器 SSH 端口'
$runId = 'data-v2-abl-g-oristrip-b16-s42'
New-Item -ItemType Directory -Path '.\exports' -Force | Out-Null
scp -P $sshPort `
  "root@${sshHost}:/root/yolo_plus/exports/${runId}.zip" `
  '.\exports\'
```

ZIP 下载完成后，在本机同一项目根目录解包：

```powershell
python scripts/transfer_run.py unpack `
  --archive "exports/${runId}.zip"
```

本地已有同名 Run 时不重复解包。完整 Run 保存在 `runs/`，代码仓库只同步轻量记录。

## 8. 结果返回后

按官方 `Box mAP50-95 + Mask mAP50-95` fitness 选择 `best.pt`，比较该权重对应的 Val Mask mAP50-95。

| 对比 | 判断依据 |
|---|---|
| G 对 `data-v2-abl-000-y26m-b16-s42` | Val Mask mAP50-95 > 0.36348 判定为正向；margin 小于一个 epoch 的波动时不判定 |
| 机制验证 | 分别报出卷叶螟与钻心虫的 Mask mAP50-95 及 mask P/R，检验第 2 节的预期方向 |
| 融合一致性 | 用 `best.pt` 走一次 `model.fuse()` 后的前向，确认对角掩码分支与其余分支一致（本地聚焦测试已覆盖，此处只做落地确认） |

本轮的记录分路：属结构消融，分析写到 `experiment_records/runs/<run-id>.md`，运行 `fill_results_table.py` 回填 `comparison.csv`，性能进**表 1**；不进表 2。

Test 留到全部方案冻结后统一评估。

### 本轮实际判定

| 项目 | 结果 |
|---|---|
| 门控 | 未通过。Val Mask mAP50-95=0.36020，低于 0.36348 且差额小于单轮波动中位数，判为与 Baseline 不可区分 |
| 机制验证 | 预期被否。两类 Mask AP50 同幅下降 0.015，卷叶螟 Mask mAP50-95=0.266 与 Baseline 逐位相同 |
| 融合一致性 | 训练收尾日志已用 `fuse=True` 重载 `best.pt` 完成整轮 Val，融合后 156 层 / 23,625,426 参数 / 122.4 GFLOPs，两条对角分支权重非零，未出现异常 |
| 权重迁移 | 入口 904/942、训练器重建后 928/942，未迁移项只有新模块 38 个张量与 80→2 类头 14 个张量 |

卷叶螟的细长几何这一路（F、F2、G）至此已有三条独立负结果，后续结构改进换到别的机制，不在 P3 条带方向上继续加码。
