# 实验步骤：云服务器训练操作手册

> 用途：本地代码/配置提交推送到 GitHub 之后，在云服务器上手动执行的操作步骤与命令。
> 本文件只写「这次要在云端跑什么、按什么顺序、敲什么命令」，不重复实验设计与记录表里的规则。

## 0. 本次任务概要

| 项目 | 值 |
|---|---|
| 分支 | `cloud/data-v2-5090` |
| Run ID | `data-v2-tune-mr2-nomix-e300-b16-s42` |
| 模型 | `yolo26m-seg.pt` |
| 数据 | `rice-pest-data-v2`（云端 `/root/yolo_data`） |
| 配置文件 | `experiments/yolo26m_seg_5090.yaml` |
| 继承 P1 | epochs=300、batch=16、imgsz=640、degrees=15、flipud=0.5、scale=0.3 |
| 本轮改动 | mask_ratio=2、mixup=0；显式锁定 AdamW、lr0=0.001667、momentum=0.9、weight_decay=0.0005 |

**前置**：本地已完成「配置修改 → 提交 → 推送」，远端分支 `cloud/data-v2-5090` 已包含上述改动。以下步骤从云服务器 `git pull` 开始。

本轮与 P1 `data-v2-tune-e300-b16-s42` 比较，P1 参考值为：official fitness 0.80993、Mask mAP50 0.68965、Mask mAP50-95 0.35805。优先按官方 fitness 选择方案，同时保护 Mask mAP50 和卷叶螟分类型结果。单 seed 差异小于约 0.005 时先按近似持平处理，不直接宣称提升。

---

## 1. 进入仓库目录并拉取最新代码

```bash
cd yolo_plus              # 或之前 clone 时的实际目录名
git pull --ff-only
```

确认拉取成功、没有冲突；`git log --oneline -1` 应看到本地刚推送的那条 commit。

```bash
git branch --show-current     # 应为 cloud/data-v2-5090
```

## 2. 确认数据挂载

```bash
ls /root/yolo_data            # 应包含 images/ 和 labels/
```

如果缺 `images/` 或 `labels/`，先按 `experiments/yolo_data_v2_cloud.yaml` 的约定放好数据再继续。

## 3.（可选）打印最终参数，不训练

正式训练前可以先 dry-run 一次，确认参数与预期一致：

```bash
python scripts/train_yolo26_seg.py \
  --config experiments/yolo26m_seg_5090.yaml \
  --run-name "data-v2-tune-mr2-nomix-e300-b16-s42" \
  --dry-run
```

应能看到以下关键参数：

```text
epochs=300, batch=16, imgsz=640, patience=100
degrees=15, flipud=0.5, scale=0.3
mask_ratio=2, mixup=0.0
optimizer=AdamW, lr0=0.001667, momentum=0.9, weight_decay=0.0005
warmup_epochs=3, cos_lr=false, lrf=0.01
```

确认无误后再由用户手动启动训练。

> 若换用了新的 GPU 实例，可先跑 `--preflight10`（把 epochs 临时改为 10）做一次兼容性检查，再跑正式训练。

## 4. 启动正式训练

```bash
python scripts/cloud_train_data_v2.py --run-name "data-v2-tune-mr2-nomix-e300-b16-s42"
```

要点：

- 训练输出目录为 `runs/data-v2-tune-mr2-nomix-e300-b16-s42/`。
- P1 的 300 epoch 用时约 1.063 小时；本轮 mask_ratio=2 会增加掩膜损失计算量，实际时长预计略高，以云端日志为准。
- `best.pt` 与 EarlyStopping 使用 Ultralytics 官方分割 fitness（Box mAP50-95 + Mask mAP50-95），`patience=100` 可能让训练提前结束。
- 若训练 loss、mAP 指标、模型权重出现 `NaN/Inf`，或发生 `CUDA out of memory`，立即中断并保留日志。
- P1 曾有 24 个 epoch 的部分 Val loss 字段为 NaN，但核心指标和权重有限；若 P2 仅复现同类 Val loss 日志异常，先记录具体 epoch 和字段，不要在未确认指标/权重受污染前盲目删除 Run。

**建议后台运行**（训练时间较长，避免 SSH 断连中断）：

```bash
nohup python scripts/cloud_train_data_v2.py --run-name "data-v2-tune-mr2-nomix-e300-b16-s42" > train_data-v2-tune-mr2-nomix-e300-b16-s42.log 2>&1 &
```

查看进度：

```bash
tail -f train_data-v2-tune-mr2-nomix-e300-b16-s42.log
```

## 5. 训练期间监控（可选）

```bash
nvidia-smi                   # mask_ratio=2 的显存会高于 P1 的约 13.3 GB，32 GB 5090 应有余量
```

确认没有 OOM、显存没有异常飙升。

## 6. 训练完成后打包 Run

等训练进程结束后（日志出现完成信息，或 `nohup` 进程退出），执行：

```bash
python scripts/transfer_run.py pack --run-id "data-v2-tune-mr2-nomix-e300-b16-s42"
```

成功后会输出 `[ARCHIVE] exports/data-v2-tune-mr2-nomix-e300-b16-s42.zip`。**务必等训练完全结束再打包**，否则 ZIP 不完整。

## 7. 下载 ZIP 回本地

把 `exports/data-v2-tune-mr2-nomix-e300-b16-s42.zip` 下载到本地电脑（scp 或云平台文件下载功能），放到本地仓库的 `exports/` 目录下。

---

## 8. 本地接收（回到本地电脑执行）

```powershell
python scripts/transfer_run.py unpack --archive "exports/data-v2-tune-mr2-nomix-e300-b16-s42.zip"
```

解包到 `runs/data-v2-tune-mr2-nomix-e300-b16-s42/`，之后：

1. 写 `experiment_records/parameter_tuning/data-v2-tune-mr2-nomix-e300-b16-s42.md` 分析；
2. 把结果追加到 `云服务器实验设计与记录表.md` 表 2；
3. **不要**运行 `fill_results_table.py`（参数优化 Run 不进入 `comparison.csv`）。

## 9. 注意事项

- 云服务器是「一次性训练节点」：只拉取已提交版本训练并打包，不在云端改配置、不填实验表。
- 训练、打包过程中不要覆盖同名 `runs/` 目录或 ZIP；换参数时必须换新 Run ID。
- 若 `git pull` 提示分支已分离或需要 rebase，不要强推，先把本地改动提交后再拉取。
