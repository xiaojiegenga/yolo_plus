# 训练结果图表分析 — yolo26m-seg 混合数据（768/95/95）

> **训练日期**: 2026-06-28 | **Best epoch**: 246 | **Val Mask mAP50**: 0.683 | **Test Mask mAP50**: 0.648  
> 图表位置: `runs/segment/runs_seg/yolo26m_seg_20260628_172809/`

---

## 1. results.png — 训练曲线总览

![results.png](runs/segment/runs_seg/yolo26m_seg_20260628_172809/results.png)

这是最重要的图,包含训练全程的所有指标变化。分三部分解读:

### 第一行:训练损失 (Loss)

| 曲线 | 起点→终点 | 解读 |
|---|---|---|
| `train/box_loss` | 2.7 → 1.5 | 边框回归损失持续下降,无反弹,说明边界框定位在稳定改善 |
| `train/seg_loss` | 3.6 → 1.3 | **最平滑的曲线**,mask 分割是模型学得最好的部分,收敛稳定 |
| `train/cls_loss` | 7.0 → 1.7 | 分类损失降幅最大,后期趋于平缓,两类区分已学到较高水平 |
| `train/dfl_loss` | 0.013 → 0.006 | 分布焦点损失,数值极小,对整体优化贡献有限 |
| `train/sem_loss` | 6.1 → 1.3 | yolo26 独有的语义辅助头损失,帮助 backbone 学到更好的特征表达 |

**训练状态判断**:五条 loss 全部持续下降且未反弹 → **没有过拟合**。seg_loss 最平滑,说明 mask 分割是模型的强项;cls_loss 降幅最大,分类准确度提升明显。

### 第二行:验证指标 — Box & Mask Precision/Recall

| 曲线 | 特征 | 解读 |
|---|---|---|
| `metrics/precision(B)` | **极度振荡**(0.2↔0.8) | ❗ 这不是模型问题,而是 val 集太小(95 张)导致的统计噪声。每 batch 的 P/R 波动大是正常的 |
| `metrics/recall(B)` | 振荡但幅度较小(0.3↔0.7) | 召回比精确率稍稳定,模型倾向"宁可多框不可漏" |
| `metrics/precision(M)` | 同 Box P,大幅振荡 | Mask 精确率同样受小 val 集影响,只看趋势不看单点 |
| `metrics/recall(M)` | 0.4↔0.7,趋势缓慢上升 | Mask 召回率,见下一行 mAP 的综合评估 |

**关键认知**:验证集只有 95 张图(85 正 + 10 负),batch 验证时每批样本少 → P/R 曲线必然振荡。**不要盯着 P/R 的毛刺看,看 mAP50(M) 的趋势线就好**,mAP 已经综合了 P 和 R。

### 第三行:验证指标 — mAP & 学习率

| 曲线 | 起点→终点/Best | 解读 |
|---|---|---|
| `metrics/mAP50(B)` | 0→~0.62 | Box 检测 mAP,持续上涨到 epoch 200 后趋于平稳 |
| `metrics/mAP50-95(B)` | 0→~0.35 | 更严格的 Box mAP(IOU 0.5→0.95 平均),后期仍有微弱上升 |
| **`metrics/mAP50(M)`** | **0→0.683** | ★ **最重要的指标** — Mask 分割 mAP50。epoch 0–100 快速上涨,100–200 缓慢爬坡,200+ 近乎 plateau。Best 出现在 epoch 246 |
| `metrics/mAP50-95(M)` | 0→~0.33 | 严格 Mask mAP,上涨趋势与 mAP50 一致但绝对值低约一半,说明高 IOU 阈值下 mask 边缘精度仍有提升空间 |
| `lr/pg0, pg1, pg2` | 余弦退火 | warmup 5 epoch → 峰值 ~0.01 → 余弦衰减至 ~1e-4,标准的 OneCycleLR 策略 |

### 综合结论

- ✅ 模型训练健康:无过拟合、无梯度爆炸、loss 持续下降
- ✅ Mask mAP50 在 epoch 100 后进入缓慢提升区,说明数据量足够支撑进一步优化
- ⚠️ P/R 振荡源于 val 集偏小,但不影响 best.pt 的选择(mAP 是综合指标)
- 💡 mAP50-95 只有 mAP50 的一半 → mask 边缘精细度是后续优化方向(分割头改进)

---

## 2. MaskPR_curve.png — 分割 PR 曲线

![MaskPR_curve.png](runs/segment/runs_seg/yolo26m_seg_20260628_172809/MaskPR_curve.png)

**怎么读**:横轴 Recall(找回率),纵轴 Precision(准确率)。曲线越靠右上角越好,曲线下面积 = mAP。

| 类别 | mAP50 | 曲线特征 | 解读 |
|---|---|---|---|
| 全部(all) | **0.683** | 黑色粗线 | 综合 PR,整体在 0.5–0.7 区间 |
| Rice leaffolder | **0.604** | 蓝色线,中段下坠明显 | 召回 >0.5 后精确率快速下滑→密集小目标中有些是假的,误检集中在中等召回段 |
| Rice stemborers | **0.763** | 橙色线,全程高于蓝色 | 高召回(>0.7)时精确率仍 >0.7,钻心虫特征明确,误检少 |

**关键洞察**:两条曲线在高召回段(右端)虽然都下降但程度不同——钻心虫在 R=0.8 时 P≈0.7,卷叶螟在 R=0.6 时 P≈0.5。**后续优化重点是把卷叶螟的蓝色曲线往上拉**(减少密集小目标的误检)。

---

## 3. MaskF1_curve.png — F1 分数 vs 置信度阈值

![MaskF1_curve.png](runs/segment/runs_seg/yolo26m_seg_20260628_172809/MaskF1_curve.png)

**怎么读**:横轴是置信度阈值(conf),纵轴是 F1 分数(P 和 R 的调和平均)。**曲线最高点对应的 conf 就是最佳推理阈值。**

| 类别 | 最佳 F1 | 最佳 conf | 解读 |
|---|---|---|---|
| 全部 | ~0.64 | **~0.36** | 综合最佳置信度 ≈ 0.35 |
| Rice leaffolder | ~0.57 | **~0.35** | F1 不高,conf >0.5 后 F1 崩溃——卷叶螟置信度天生偏低 |
| Rice stemborers | ~0.72 | **~0.38** | F1 很高,conf 0.2–0.7 区间内都稳定——钻心虫识别很自信 |

**实用结论**:
- **演示/发布**:conf 设 **0.45**(卷叶螟) / **0.30**(钻心虫),分别平衡各类的 P/R
- **调参依据**:卷叶螟 F1 峰值只有 0.57 vs 钻心虫 0.72,差距来自卷叶螟召回不足→后续改进重点
- 钻心虫的 F1 曲线平坦——即使在 conf=0.5 下仍保持高 F1,说明模型对钻心虫的预测置信度很高

---

## 4. confusion_matrix_normalized.png — 归一化混淆矩阵

![confusion_matrix_normalized.png](runs/segment/runs_seg/yolo26m_seg_20260628_172809/confusion_matrix_normalized.png)

**怎么读**:行为真实类别,列为预测类别。对角线(正确分类)越亮越好,非对角线(误分类)越暗越好。

| 真实 \ 预测 | leaffolder | stemborers | background |
|---|---|---|---|
| **leaffolder** | **0.48** | 0.00 | **0.52** |
| **stemborers** | 0.00 | **0.78** | **0.22** |
| **background** | 0.00 | 0.00 | 1.00 |

### 关键解读

**问题不在类间混淆,而在漏检(把目标当背景)**:
- 卷叶螟:**52% 被漏掉**(预测为 background)。这是模型当前最大的短板——小目标、密集、症状模糊,模型不敢框
- 钻心虫:22% 被漏掉,远好于卷叶螟。穗尖白枯特征明确,模型信心足
- **类间混淆为 0%** — leaffolder ↔ stemborers 完全不会互相认错,分类能力没问题

**结论**:
- ✅ 分类准确度极好,两类不会相互串扰
- ❌ 卷叶螟漏检率 52% 是瓶颈,需要从"让模型更敢框小目标"入手:
  - 注意力模块让模型关注病斑区域(提升 recall)
  - 降低推理 conf 阈值(但会增加误检,需要配合 NMS)
- 背景从未被误判为病害 → 负样本训练有效

---

## 5. BoxPR_curve.png & BoxF1_curve.png — Box 检测指标(参考)

这两张是 Box(边框)维度的 PR 和 F1 曲线,在实例分割任务中参考价值次于 Mask 指标。

| 类别 | Box mAP50 | 特征 |
|---|---|---|
| 全部 | 0.667 | 略低于 Mask(0.683),正常——分割任务中 mask 监督信号强于 box |
| leaffolder | 0.583 | PR 曲线形态与 Mask 类似,中召回段下坠 |
| stemborers | 0.751 | 高召回高精确,钻心虫检测基本可靠 |

Box F1 最优阈值与 Mask 一致(~0.35–0.38),说明模型对两类任务的置信度分布一致。

---

## 总结与后续方向

### 当前模型状态

| 维度 | 评价 |
|---|---|
| 训练收敛 | ✅ 健康,无过拟合 |
| 分类能力 | ✅ 两类 100% 不串扰 |
| 钻心虫识别 | ✅ mAP50=0.76,漏检率 22%,基本可用 |
| 卷叶螟识别 | ⚠️ mAP50=0.60,漏检率 52%,需要提升 |
| Mask 边缘精度 | ⚠️ mAP50-95 仅 mAP50 的一半,边缘不够精细 |

### 对应改进方向

1. **卷叶螟漏检**(最大痛点) → Neck 加入 P2 高分辨率特征层,让小目标有更大的特征图
2. **Mask 边缘粗糙** → 改进分割 head(轻量 decoder)或改用 Dice Loss
3. **卷叶螟特征提取弱** → backbone 嵌入 CBAM 注意力,让模型关注病斑区域
4. **推理阈值** → 卷叶螟用 conf=0.35,钻心虫用 conf=0.30,按 F1 曲线选最优

---

*图表分析完成时间: 2026-06-29 | 基于训练权重: yolo26m_seg_20260628_172809*
