# 云服务器实验项目交接

更新日期：2026-09-04

## 当前工作方式

本地电脑是唯一开发和分析环境，GitHub 用于同步已提交的代码、配置和轻量记录，云服务器
只负责拉取、训练和打包：

```text
本地修改并 push → 云端 clone/pull → 云端训练并打包 → 本地接收、分析和记录
```

仓库继续使用 `xiaojiegenga/yolo_plus`。

| 用途 | 分支 / 提交 | 状态 |
|---|---|---|
| RTX 5090 正式 Baseline | `cloud/data-v2-5090` / `c0f4f35` | `000` 已训练并完成本地记录 |
| 改进 A1/A2 | `feature/data-v2-abl-a-attention` | A1 已完成；A2 正式结果待回传 |
| 改进 B：Instance Dice | `feature/data-v2-abl-b-dice` / 本次提交 | 本地实现与测试完成；待云端预检和正式训练 |

## 当前实验

| 项目 | 固定值 |
|---|---|
| 任务 | 无人机航拍水稻害虫实例分割 |
| 数据集 | `rice-pest-data-v2` |
| 类别 | `Rice leaffolder`、`Rice stemborers` |
| 图片数 | train / val / test = 938 / 117 / 118 |
| 实例数 | 卷叶螟 4027；钻心虫 1110 |
| GPU | RTX 5090 |
| 当前开发分支 | `feature/data-v2-abl-b-dice` |
| B 源码与配置 commit | `1d1a71e` |
| 当前正式 Run ID | `data-v2-abl-010-dice-b16-s42` |
| 当前配置 | `experiments/data-v2-abl-010-dice-b16-s42.yaml` |
| 当前模型 | 官方 YOLO26m-seg，模型结构不变 |
| 当前损失变量 | 实例掩膜 BCE + 0.5 × Soft Dice |
| 预训练权重 | 官方 `yolo26m-seg.pt` |
| 正式训练 | epochs=300，batch=16，imgsz=640，seed=42 |
| 方案选择 | Val；Test 保留到最终方案冻结后统一使用 |

B 分支从正式 Baseline `c0f4f35` 创建，不包含 A1/A2 注意力源码。B 只在原实例掩膜 BCE
上增加框内 Soft Dice；检测损失、语义辅助损失、模型结构、推理流程、数据和训练配方不变。

| B 参数 | 冻结值 |
|---|---:|
| `instance_dice_gain` | 0.5 |
| `instance_dice_smooth` | 1.0 |
| sigmoid | 只对 Dice 的 mask logits 使用 |
| 空间范围 | 每个匹配实例的目标框内 |
| 聚合 | 各正样本实例相加，再沿用原 `fg_mask.sum()` 归一化 |

## 云服务器前置条件

1. A2 训练已经结束，不再有依赖当前 Python 进程的分支操作；
2. 镜像已经安装可用的 CUDA 版 PyTorch；
3. `/root/yolo_data` 直接包含 `images/` 和 `labels/`；
4. 云端仓库没有未提交的配置或源码改动；
5. 能访问 GitHub，且官方 `yolo26m-seg.pt` 已存在或可以下载。

其他 Ultralytics 依赖缺失时，`scripts/cloud_train_data_v2.py` 会安装仓库内源码。

## 云端执行入口

A2 结束后，云端切换到 B 分支，先 dry-run，再运行 10 epoch 预检；完整命令见
`实验步骤.md`。预检通过后，300 epoch 正式训练仍由用户手动启动。

```text
A2 训练结束并打包
→ 切换 feature/data-v2-abl-b-dice
→ B dry-run
→ B 10 epoch 预检
→ 用户手动启动 B 正式训练
→ 打包并回传本地分析
```

10 epoch 预检只检查配置传递、前向/反向、Val、显存和保存流程，不进入实验总表或
`comparison.csv`。

## 本地接收与记录

B 正式结果回传前，不创建正式结果记录，也不提前写入 `comparison.csv`。回传后读取
`runs/data-v2-abl-010-dice-b16-s42/` 中的 `args.yaml`、`results.csv`、`best.pt` 和训练
日志，建立 `experiment_records/runs/data-v2-abl-010-dice-b16-s42.md` 并回填正式表格。

数据、完整 `runs/`、传输 ZIP 和模型权重不进入 GitHub。当前流程不生成哈希、manifest
或备份证明。
