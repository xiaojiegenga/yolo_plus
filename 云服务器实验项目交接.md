# 云服务器实验项目交接

更新日期：2026-08-28

## 当前工作方式

本地电脑是唯一开发和分析环境，GitHub 是代码与配置的中转站，云服务器是一次性
训练节点：

```text
本地修改并 push → 云端 clone/pull → 云端训练并打包 → 本地接收、分析和记录
```

继续使用原仓库 `xiaojiegenga/yolo_plus`，当前分支为
`cloud/data-v2-5090`，接续点为 `origin/data-v2/main@42f0651`。云训练流程、配置和
说明已提交并推送至 `origin/cloud/data-v2-5090@60f070a`。RTX 5090 已确定；首轮参数
优化分析和表 2 改版仍在本地工作区，尚未提交或推送。

## 研究任务

- 任务：无人机航拍水稻害虫实例分割。
- 数据集：`rice-pest-data-v2`。
- 类别：`Rice leaffolder`、`Rice stemborers`。
- 图片：train / val / test = 938 / 117 / 118。
- 实例：卷叶螟 4027，钻心虫 1110。
- 模型选择：Ultralytics 官方分割 fitness（Val Box mAP50-95 + Val Mask mAP50-95）；论文同时报告两项 Mask mAP。
- Test：方案确定后统一评估，不用于调参。

旧仓库卷叶螟分项为 3145 / 462 / 420，合计 4027；用户已确认该口径正确。

## GitHub 中保存的内容

- 模型源码 `ultralytics-main/`；
- 实验参数 `experiments/`；
- 训练和传输脚本 `scripts/`；
- 正式训练的轻量记录 `experiment_records/`；
- 项目说明和实验总表。

数据、完整 `runs/`、传输 ZIP 和模型权重不进入 GitHub。

## 云服务器前置条件

云端克隆仓库后只剩两个外部条件：

1. 镜像已经安装可用的 CUDA 版 PyTorch；
2. `/root/yolo_data` 已直接包含 `images/` 和 `labels/`。

其他 Ultralytics 依赖缺失时，`scripts/cloud_train_data_v2.py` 会安装仓库内源码。
官方预训练权重本地不存在时，由 Ultralytics 首次下载；若服务器不能联网，需要事先
把权重放到仓库工作目录外并通过 `--model` 指定。

## 云端命令

首次克隆：

```bash
git clone --branch cloud/data-v2-5090 https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd yolo_plus
```

下一轮训练配置（第 1 轮组合）已写入 `experiments/yolo26m_seg_5090.yaml`（Run ID
`data-v2-tune-e300-b16-s42`：epochs=300、degrees=15、flipud=0.5、scale=0.3，batch
保持 16、分辨率固定 640）。本地提交并推送后，云端执行：

```bash
git pull --ff-only
RUN_ID="data-v2-tune-e300-b16-s42"
python scripts/cloud_train_data_v2.py --run-name "${RUN_ID}"
```

正式训练参数当前尚未冻结，不得把现有参数直接视为最终方案。

## 本地接收与分析

训练归档下载后，根据本地 `runs/<run-id>/args.yaml`、`results.csv`、权重和验证输出处理：

- 参数优化 Run：写 `experiment_records/parameter_tuning/<run-id>.md`，并回填总表表 2；
- 参数冻结后的期刊正式 Run：写 `experiment_records/runs/<run-id>.md`，并回填 `comparison.csv`；
- `云服务器实验设计与记录表.md`：始终只保存计划、参数、状态和结果表格。

完整 Run 长期保存在本地或外部磁盘；`exports/<run-id>.zip` 只是传输载体，可按用户
自己的存储策略保留。

参数优化结果不写 `comparison.csv`。10 epoch 预检既不写 `comparison.csv`，也不创建
参数优化或期刊正式 Run 分析。

## 历史 data-v2 Baseline

`data-v2-baseline-b8` 来自旧环境，只作为历史参照。

| 项目 | 结果 |
|---|---:|
| batch | 8 |
| 实际 / 最佳 epoch | 359 / 259 |
| Val Mask P / R | 0.694 / 0.605 |
| Val Mask mAP50 / mAP50-95 | 0.708 / 0.374 |
| 参数量 / GFLOPs | 23.509M / 121.2 |
| 训练时长 | 4.216 h |

历史记录位于 `experiment_records/data-v2-baseline-b8/`。旧原始 Run 尚未迁入
本地工作区，不得把它写成新 5090 环境结果。

## 当前参数优化进展

- 云端数据集已经放在 `/root/yolo_data`。
- RTX 5090 已确定，不再运行 4090 预检。
- `data-v2-scale-y26m-seg-b16-s42` 已完成：实际 epoch 为 292；官方 fitness 在 epoch 192 达到 0.78033，对应现有 `best.pt`。
- Mask mAP50 的曲线峰值为 epoch 252 / 0.70050，但该轮权重未保存，只作为补充曲线信息；后续训练继续按官方 fitness 保存 `best.pt` 和执行 EarlyStopping。
- 原始结果位于 `runs/data-v2-scale-y26m-seg-b16-s42/`。
- 分析位于 `experiment_records/parameter_tuning/data-v2-scale-y26m-seg-b16-s42.md`。
- 本次结果只用于表 1 调参，已填入总表表 2，不进入 `comparison.csv`。
- 第 1 轮组合优化方案已确定并写入 `experiments/yolo26m_seg_5090.yaml`：epochs 400→300、新增 degrees=15/flipud=0.5/scale=0.3，batch 保持 16（为后续消融单变量原则统一 batch=16）、分辨率固定 640（不做 832）；Run ID `data-v2-tune-e300-b16-s42`，待提交推送后云端训练。

## 下一步

1. 提交并推送配置（`experiments/yolo26m_seg_5090.yaml` 与 `PROGRESS.md`）；云端拉取后运行 `data-v2-tune-e300-b16-s42`。
2. 回传后新增参数优化分析，并把结果追加到总表表 2。
3. 第 2 轮（掩膜精调组合）：`mask_ratio 4→2`、`warmup_epochs 3→5`（分辨率固定 640，不做 imgsz 实验）。
4. 参数方案确定后做多 seed 复验，冻结后填写总表表 1，再开始期刊正式实验。

当前流程不要求哈希、manifest、备份证明或额外审计目录。
