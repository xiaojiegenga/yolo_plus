# 云服务器实验项目交接

更新日期：2026-09-10。

## 当前接续点

当前开发分支 `feature/data-v2-abl-drb-scsa`，从正式 Baseline `c0f4f35` 建立。
已实现全部 8 个 C3k2 的 DRB 替换，并在原第 4、6 层后独立加入零初始化残差 SCSA。
模型保留 C2PSA、末级 PSA、原始 Segment26/Proto。完整结构作为一个候选与 `000` 比较。

- Run ID：`data-v2-abl-drb-scsa-p34-b16-s42`。
- 配置：`experiments/data-v2-abl-drb-scsa-p34-b16-s42.yaml`。
- 教学：`knowledge/全C3k2-DRB与P3P4-SCSA原理与实现.md`。
- 操作：`实验步骤.md`。
- 状态：7 项 CPU 测试通过；CUDA 检查、10 epoch 预检、正式训练尚待用户在云端执行。

A–E 完整记录保留在 `feature/data-v2-abl-a-attention`。2026-09-09 交接中，D1 的
Mask mAP50-95 为 0.37327，保留候选；E 为 0.34649，当前不组合。本分支不混入其他改进源码。
本分支历史总表继承 Baseline 快照，当前候选填总表表 18；完整近期记录以分析分支为准。

## 固定研究与训练信息

- 任务：无人机航拍水稻害虫实例分割，数据集 `rice-pest-data-v2`。
- 类别：`Rice leaffolder`、`Rice stemborers`。
- 图片：train / val / test = 938 / 117 / 118。
- 实例：卷叶螟 4027，钻心虫 1110；卷叶螟分项为 3145 / 462 / 420。
- 正式 GPU：RTX 5090。
- P2 配方已冻结：epochs=300、patience=100、batch=16、imgsz=640、seed=42、AdamW、
  lr0=0.001667、mask_ratio=2、mixup=0；当前配置的全部 train 参数与正式 000 相同。
- 预训练：官方 `yolo26m-seg.pt`；不从其他实验 best.pt 接着训练。
- best.pt 与 EarlyStopping：官方分割 fitness，即 Val Box mAP50-95 + Val Mask mAP50-95。
- 方案主指标：Val Mask mAP50-95；同时报告 Mask mAP50。
- Val 选方案，Test 在最终方案冻结后统一评估；本轮固定 seed=42。

## 三地分工

本地修改和提交 → GitHub 同步 → 云端拉取并训练 → 打包 ZIP → 本地 SCP 接收、解包与分析。
云端不维护独立配置或实验表；本地 `runs/<run-id>/` 长期保存完整原始结果。
GitHub 只保存源码、配置、脚本、教学与轻量实验记录，不保存数据、权重、完整 Run 和 ZIP。

## 云端准备与启动

云端镜像已安装可用的 CUDA PyTorch，数据位于 `/root/yolo_data/images/` 和 `labels/`。
其他依赖缺失时，`scripts/cloud_train_data_v2.py` 安装仓库内 Ultralytics。
官方预训练权重自动下载；离线时提前放好官方权重，并通过 `--pretrained` 指定其路径，
模型仍使用本轮结构 YAML，不能用 `--model` 换回官方网络。

严格按 `实验步骤.md`：拉取新分支 → 显式配置 dry-run → CUDA 检查 → 10 epoch 预检 →
用户手动正式训练。7 项检查不读取数据集；预检不进入论文排名，不建立记录。
正式训练日志放入 Run 后一起打包，SCP 的主机和端口使用当前云实例控制台提供的值。

## 本地记录

- 本轮正式 Run 回传后，在 `experiment_records/runs/data-v2-abl-drb-scsa-p34-b16-s42.md` 写分析，
  回填总表表 18 和 `comparison.csv`，数据版本为 `data-v2`。
- 参数优化历史 Run 仍只写 `parameter_tuning/` 和表 2，不进入正式汇总。
- 总表只保存计划、参数、状态和性能数据，结论写入单次分析。
- 不覆盖已有 Run、ZIP、权重或历史记录；重跑使用新 Run ID。
- 当前不填新模型性能，不启动正式训练，不进行 Test 调参。

## 已有基准

| 基准 | Best epoch | Mask mAP50 | Mask mAP50-95 | 用途 |
|---|---:|---:|---:|---|
| 正式 `data-v2-abl-000-y26m-b16-s42` | 216 | 0.71132 | 0.36348 | 当前统一结构对照 |
| 调参 P2 `data-v2-tune-mr2-nomix-e300-b16-s42` | 216 | 0.71132 | 0.36348 | 冻结配方来源，不进正式比较 |
| 历史 `data-v2-baseline-b8` | 259 | 0.708 | 0.374 | 旧环境历史参照 |

正式 000 完成 300 epoch，official fitness=0.81977；与 P2 在 CSV 保存精度下复现。
历史 b8 训练 359 epoch，4.216 h，23.509M / 121.2 GFLOPs，记录在
`experiment_records/data-v2-baseline-b8/`，不视为当前 5090 配方结果。

下一步先运行本轮完整结构候选；结果回传后判断效果，再决定是否与 D1 组合。
