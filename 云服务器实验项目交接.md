# 云服务器实验项目交接

更新日期：2026-09-03

## 当前工作方式

本地电脑是唯一开发和分析环境，GitHub 用于同步已提交的代码、配置和轻量记录，云服务器
只负责拉取、训练和打包：

```text
本地修改并 push → 云端 clone/pull → 云端训练并打包 → 本地接收、分析和记录
```

仓库继续使用 `xiaojiegenga/yolo_plus`。

| 用途 | 分支 / 提交 | 状态 |
|---|---|---|
| RTX 5090 正式 Baseline | `cloud/data-v2-5090` / `c0f4f35` | `000` 已训练并完成本地记录 |
| 改进 A：SR-CBAM | `feature/data-v2-abl-a-attention` / `9d0c479` | 正式训练和本地分析完成；未通过门控 |

## 当前实验

| 项目 | 固定值 |
|---|---|
| 任务 | 无人机航拍水稻害虫实例分割 |
| 数据集 | `rice-pest-data-v2` |
| 类别 | `Rice leaffolder`、`Rice stemborers` |
| 图片数 | train / val / test = 938 / 117 / 118 |
| 实例数 | 卷叶螟 4027；钻心虫 1110 |
| GPU | RTX 5090 |
| 当前阶段 | 改进 A 已结束；下一步冻结并实现改进 B：Dice |
| 最近正式 Run ID | `data-v2-abl-100-srcbam-b16-s42` |
| 最近配置 | `experiments/data-v2-abl-100-srcbam-b16-s42.yaml` |
| 最近模型 | YOLO26m-seg + Backbone P3/P4 SR-CBAM |
| 预训练权重 | 官方 `yolo26m-seg.pt` |
| 正式训练 | epochs=300，batch=16，imgsz=640，seed=42 |
| 方案选择 | Val；Test 保留到最终方案冻结后统一使用 |

改进 A 相对正式 `000 Baseline` 只替换 Backbone 第 4、6 层为 `C3k2SRCBAM`，其余模型
结构、损失、数据和训练参数不变。原理与源码说明位于
`knowledge/改进A-SR-CBAM注意力机制原理与实现.md`。

| 结果 | 000 Baseline | A：SR-CBAM | 差值 |
|---|---:|---:|---:|
| Official fitness | 0.81977 | 0.80644 | -0.01333 |
| Mask mAP50 | 0.71132 | 0.70884 | -0.00248 |
| Mask mAP50-95 | 0.36348 | 0.35097 | -0.01251 |

当前 A 未通过预设单模块门控，不进入 A+B、A+C 或 A+B+C 组合。B 必须从正式 Baseline
提交 `c0f4f35` 建立兄弟分支，不能从 A 分支继续开发。

## 云服务器前置条件

1. 镜像已经安装可用的 CUDA 版 PyTorch；
2. `/root/yolo_data` 直接包含 `images/` 和 `labels/`；
3. 云端仓库没有未提交的临时配置；
4. 能访问 GitHub；首次使用官方权重时能下载 `yolo26m-seg.pt`。

其他 Ultralytics 依赖缺失时，`scripts/cloud_train_data_v2.py` 会安装仓库内源码。

## 云端执行入口

当前没有已冻结、可直接启动的下一次云端正式训练。`实验步骤.md` 保存的是已完成改进 A 的
复现命令。下一次云端操作必须等本地完成 B 的公式冻结、源码、配置、测试、提交和推送后再编写。

```text
冻结 B 的唯一公式与超参数
→ 从 cloud/data-v2-5090@c0f4f35 创建 B 的兄弟分支
→ 本地实现、测试并推送
→ 云端 dry-run 和 10 epoch 预检
→ 用户确认后手动启动正式训练
```

10 epoch 预检只检查权重迁移、前向/反向、Val、显存和保存流程，不进入实验总表或
`comparison.csv`。

## 本地接收与记录

改进 A 的完整 Run 和训练日志已位于
`runs/data-v2-abl-100-srcbam-b16-s42/`；正式分析位于
`experiment_records/runs/data-v2-abl-100-srcbam-b16-s42.md`，总体与分类别结果已写入
`comparison.csv` 和实验总表。

数据、完整 `runs/`、传输 ZIP 和模型权重不进入 GitHub。当前流程不生成哈希、manifest
或备份证明。
