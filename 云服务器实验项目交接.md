# 云服务器实验项目交接

更新日期：2026-09-04

## 当前工作方式

本地电脑是唯一开发和分析环境，GitHub 用于同步已提交的代码、配置和轻量记录，云服务器
只负责拉取、训练和打包：

```text
本地修改并 push → 云端 clone/pull → 云端训练并打包 → 本地接收、分析和记录
```

仓库继续使用 `xiaojiegenga/yolo_plus`。

| 用途 | 分支 / 提交 | 状态 |
|---|---|---|
| RTX 5090 正式 Baseline | `cloud/data-v2-5090` / `c0f4f35` | `000` 已训练并完成本地记录 |
| 改进 A1：P3/P4 SR-CBAM | `feature/data-v2-abl-a-attention` / `9d0c479`、`ac11686` | 正式训练和本地分析完成；未通过门控 |
| 改进 A2：P3 ZR-CBAM | `feature/data-v2-abl-a-attention` / 最新提交 | 本地实现与测试完成；待云端预检和正式训练 |

## 当前实验

| 项目 | 固定值 |
|---|---|
| 任务 | 无人机航拍水稻害虫实例分割 |
| 数据集 | `rice-pest-data-v2` |
| 类别 | `Rice leaffolder`、`Rice stemborers` |
| 图片数 | train / val / test = 938 / 117 / 118 |
| 实例数 | 卷叶螟 4027；钻心虫 1110 |
| GPU | RTX 5090 |
| 当前阶段 | 改进 A2 已实现；下一步云端 10 epoch 预检和正式训练 |
| 当前正式 Run ID | `data-v2-abl-a2-p3-zrcbam-b16-s42` |
| 当前配置 | `experiments/data-v2-abl-a2-p3-zrcbam-b16-s42.yaml` |
| 当前模型 | YOLO26m-seg + Backbone P3 ZR-CBAM |
| 预训练权重 | 官方 `yolo26m-seg.pt` |
| 正式训练 | epochs=300，batch=16，imgsz=640，seed=42 |
| 方案选择 | Val；Test 保留到最终方案冻结后统一使用 |

改进 A1 相对正式 `000 Baseline` 只替换 Backbone 第 4、6 层为 `C3k2SRCBAM`，其余模型
结构、损失、数据和训练参数不变。A1 原理与源码说明位于
`knowledge/改进A-SR-CBAM注意力机制原理与实现.md`。

| 结果 | 000 Baseline | A：SR-CBAM | 差值 |
|---|---:|---:|---:|
| Official fitness | 0.81977 | 0.80644 | -0.01333 |
| Mask mAP50 | 0.71132 | 0.70884 | -0.00248 |
| Mask mAP50-95 | 0.36348 | 0.35097 | -0.01251 |

A1 未通过预设单模块门控，因此不直接进入组合。A2 使用新的 Run ID 独立检验两个变化：
只保留 P3 注意力，以及采用 `Y=X+β×CBAM(X)`、`β=0` 的初始恒等残差。A2 结果产生前
不决定整个注意力方向是否保留。

## 云服务器前置条件

1. 镜像已经安装可用的 CUDA 版 PyTorch；
2. `/root/yolo_data` 直接包含 `images/` 和 `labels/`；
3. 云端仓库没有未提交的临时配置；
4. 能访问 GitHub；首次使用官方权重时能下载 `yolo26m-seg.pt`。

其他 Ultralytics 依赖缺失时，`scripts/cloud_train_data_v2.py` 会安装仓库内源码。

## 云端执行入口

当前可直接执行 A2 的 dry-run 和 10 epoch 预检。完整命令见 `实验步骤.md`。预检通过后，
300 epoch 正式训练仍由用户手动启动。

```text
云端拉取 feature/data-v2-abl-a-attention 最新提交
→ A2 dry-run
→ A2 10 epoch 预检
→ 用户确认后手动启动 A2 正式训练
→ 打包并回传本地分析
```

10 epoch 预检只检查权重迁移、前向/反向、Val、显存和保存流程，不进入实验总表或
`comparison.csv`。

## 本地接收与记录

改进 A1 的完整 Run 和训练日志已位于
`runs/data-v2-abl-100-srcbam-b16-s42/`；正式分析位于
`experiment_records/runs/data-v2-abl-100-srcbam-b16-s42.md`，总体与分类别结果已写入
`comparison.csv` 和实验总表。A2 尚无正式结果，不提前写入 `comparison.csv`。

数据、完整 `runs/`、传输 ZIP 和模型权重不进入 GitHub。当前流程不生成哈希、manifest
或备份证明。
