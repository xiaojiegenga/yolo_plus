# 云服务器实验项目交接

更新日期：2026-08-27

## 当前工作方式

本地电脑是唯一开发和分析环境，GitHub 是代码与配置的中转站，云服务器是一次性
训练节点：

```text
本地修改并 push → 云端 clone/pull → 云端训练并打包 → 本地接收、分析和记录
```

继续使用原仓库 `xiaojiegenga/yolo_plus`，当前分支为
`cloud/data-v2-5090`，接续点为 `origin/data-v2/main@42f0651`。本次结构与脚本
修改尚未提交或推送；云端要使用新流程前，需先由用户在本地完成 commit 和 push。

## 研究任务

- 任务：无人机航拍水稻害虫实例分割。
- 数据集：`rice-pest-data-v2`。
- 类别：`Rice leaffolder`、`Rice stemborers`。
- 图片：train / val / test = 938 / 117 / 118。
- 实例：卷叶螟 4027，钻心虫 1110。
- 主选择指标：Val Mask mAP50-95。
- Test：方案确定后统一评估，不用于调参。

旧仓库卷叶螟分项为 3145 / 462 / 420，合计 4027；用户已确认该口径正确。

## GitHub 中保存的内容

- 模型源码 `ultralytics-main/`；
- 实验参数 `experiments/`；
- 训练和传输脚本 `scripts/`；
- 轻量记录 `experiment_records/`；
- 项目说明和实验总表。

数据、完整 `runs/`、传输 ZIP 和模型权重不进入 GitHub。

## 云服务器前置条件

云端克隆仓库后只剩两个外部条件：

1. 镜像已经安装可用的 CUDA 版 PyTorch；
2. `/root/yolo_data` 已直接包含 `images/` 和 `labels/`。

其他 Ultralytics 依赖缺失时，`scripts/cloud_train_data_v2.py` 会安装仓库内源码。
官方预训练权重本地不存在时，由 Ultralytics 首次下载；若服务器不能联网，需要事先
把权重放到仓库工作目录外并通过 `--model` 指定。

## 云端命令

首次克隆：

```bash
git clone --branch cloud/data-v2-5090 https://github.com/xiaojiegenga/yolo_plus.git yolo_plus
cd yolo_plus
```

10 epoch 预检：

```bash
python scripts/cloud_train_data_v2.py --preflight10 --run-name data-v2-5090-preflight
```

用户确认后运行正式 Baseline：

```bash
python scripts/cloud_train_data_v2.py --run-name data-v2-5090-baseline
```

训练完成后打包：

```bash
python scripts/transfer_run.py pack --run-id data-v2-5090-baseline
```

云端只需要传回 `exports/data-v2-5090-baseline.zip`。不在云端更新记录、CSV 或
Markdown，也不把 Run 推送到 GitHub。

## 本地接收与分析

下载归档后：

```powershell
python scripts/transfer_run.py unpack --archive exports/data-v2-5090-baseline.zip
python scripts/fill_results_table.py --run-dir runs/data-v2-5090-baseline --run-id data-v2-5090-baseline
```

然后根据本地 `runs/<run-id>/args.yaml`、`results.csv`、权重和验证输出填写：

- `experiment_records/runs/<run-id>.md`：保存该实验的分析文字；
- `experiment_records/comparison.csv`；
- `云服务器实验设计与记录表.md`：只回填计划、参数、状态和结果表格。

完整 Run 长期保存在本地或外部磁盘；`exports/<run-id>.zip` 只是传输载体，可按用户
自己的存储策略保留。

## 历史 data-v2 Baseline

`data-v2-baseline-b8` 来自旧环境，只作为历史参照。

| 项目 | 结果 |
|---|---:|
| batch | 8 |
| 实际 / 最佳 epoch | 359 / 259 |
| Val Mask P / R | 0.694 / 0.605 |
| Val Mask mAP50 / mAP50-95 | 0.708 / 0.374 |
| 参数量 / GFLOPs | 23.509M / 121.2 |
| 训练时长 | 4.216 h |

历史记录位于 `experiment_records/data-v2-baseline-b8/`。旧原始 Run 尚未迁入
本地工作区，不得把它写成新 5090 环境结果。

## 下一步

1. 在本地检查本次改动并推送 `cloud/data-v2-5090`。
2. 云端准备 `/root/yolo_data`。
3. 克隆分支并运行 `data-v2-5090-preflight`。
4. 把预检 ZIP 传回本地，验证完整回传流程。
5. 用户确认参数后再启动正式训练。

当前流程不要求哈希、manifest、备份证明或额外审计目录。
