# 云服务器实验项目交接

更新日期：2026-09-21

## F2 结果交接

F2 在独立分支 `feature/data-v2-abl-f2-localgate` 实现并推送，实际训练提交 `c13cef0`，配置和 Run ID 为
`data-v2-abl-f2-localgate-b16-s42`。正式训练已完成并回传：204 epoch 早停（patience=100，best epoch 104），
Val Mask mAP50-95=0.34733，较 000 低 0.01615、较原 F 低 0.01149；official fitness 0.79673，较 000
低 0.02304，**未通过门控**。检测框与掩膜两类 P/R/mAP 全部低于 Baseline；top-10 fitness 均值低
0.026658，差额大于同窗口单轮波动中位数 0.00975，不属于与 Baseline 不可区分。best.pt 中门控卷积与
投影权重全部非零、门已偏离单位门，属"有学习、无收益"。正式分析见
[F2 正式记录](experiment_records/runs/data-v2-abl-f2-localgate-b16-s42.md)，原理见
[F2 原理与实现](knowledge/改进F2-局部引导条带门控原理与实现.md)。该结果已登记 `comparison.csv`
与总表表 4、表 17、表 21。P3 条带/朝向方向（F、F2、G）至此三条独立负结果，不再继续加码。

## DRB/SCSA 结果归档

正式 Run `data-v2-abl-drb-scsa-p34-b16-s42` 已完成 300 epoch，best epoch 231。
Mask mAP50 / mAP50-95 为 0.67664 / 0.31719，已作为负结果保存在 A 总体分析分支，
详见 [结果与原因分析](experiment_records/runs/data-v2-abl-drb-scsa-p34-b16-s42.md)、总表表 19 与 `comparison.csv`。
源码与配置由 `feature/data-v2-abl-drb-scsa` 保留，实际训练提交 `3a19da2`。
下一步讨论保留通道混合、激活和局部残差的 DRB 封装；当前整体结构不直接与 D1 组合。

## F 结果交接

F 为 Neck P3 局部与条带上下文，源码位于 `feature/data-v2-abl-f-p3strip`，实际训练提交 `1590ccd`。
正式 Run `data-v2-abl-f-p3strip-b16-s42` 已完成 300 epoch，best 237；Mask mAP50 / mAP50-95=0.70420 / 0.35882，较 000 -0.00712 / -0.00466。训练及权重正常，当前不进入 D1+F，保留 D1。补充 CPU 原图配对主指标 +0.00596，小卷叶螟 AP50-95 +0.00487；存在局部收益，但不能替换正式评价标准。
[正式分析](experiment_records/runs/data-v2-abl-f-p3strip-b16-s42.md) 与总表、comparison.csv 已登记。当前分析分支仅维护文档、结果，F 源码仍在独立分支。

## E 结果交接

正式 Run `data-v2-abl-e-dysample-b16-s42` 已完成 300 epoch 并回传，best epoch 264。
Mask mAP50 / mAP50-95=0.69353 / 0.34649，较 000 -0.01779 / -0.01699；本次 E 未通过门控，不进入组合。
两个类别日志 Mask AP50-95 均下降，top-10 fitness 均值也低于 Baseline。
实际训练配方一致，best/last 指标与 CSV 一致且权重有限；仅第 2 轮部分 Val loss NaN。
实现提交 5d91a2b；日志未记录实际训练 commit。CUDA 采样反向可能非确定性。
正式记录与总表已更新，见 [E 正式分析](experiment_records/runs/data-v2-abl-e-dysample-b16-s42.md)。
总体分析在 A 分支维护，E 源码保留在独立分支。保留 D1，下一步讨论第二个结构，固定数据集与训练配方。

## D1 结果交接

正式 Run `data-v2-abl-d1-p2proto-r2-b16-s42` 已完成 300 epoch 并回传，实际训练 commit `7d277b6`，best epoch 278。
Mask mAP50 / mAP50-95 为 0.71245 / 0.37327，较 000 +0.00113 / +0.00979；保留为当前优先候选。
原图 Val 主指标 +0.00609，小卷叶螟主指标 +0.00761；固定 conf=0.25 的小卷叶螟 TP 107→118、FP 123→165。
正式记录、补充评估和总表已更新，D1 保留为两结构主线的第一个候选。
部分 Val loss 在 epoch 3、4、76、186 出现 NaN；训练损失、P/R/mAP 与最终权重有限，根因未定位。
D1 源码仍位于独立 `feature/data-v2-abl-d-p2proto`，总体分析在 A 分支维护。
读取 D checkpoint 使用 D 类定义；原始 Run 与 ZIP 保持原位。
详见 [正式分析](experiment_records/runs/data-v2-abl-d1-p2proto-r2-b16-s42.md) 与 [配对评估](experiment_records/evaluations/data-v2-d1-val.md)。

## 当前工作方式

本地开发、分析与记录 → GitHub 同步已提交代码和配置 → 云端拉取、训练和打包 → 本地接收 Run。
本次同步电脑项目根目录为 `E:\study\graduate_sec\论文撰写\模型训练`；命令均在各电脑的项目根目录执行。
本地 Python 环境和可选的 `LOCAL_SETUP.local.md` 由各电脑分别维护。

| 用途 | 分支 / 实现提交 | 当前状态 |
|---|---|---|
| 正式 000 Baseline | `cloud/data-v2-5090` / 训练 `1c63ee4`，记录 `c0f4f35` | 300 epoch，best 216，已登记 |
| A1：P3/P4 SR-CBAM | `feature/data-v2-abl-a-attention` / `9d0c479`，记录 `ac11686` | 已完成；未通过门控 |
| A2：P3 ZR-CBAM | `feature/data-v2-abl-a-attention` / `a38aabf` | 已完成并核验；276 epoch，best 176；近似持平，暂不组合 |
| B：实例 Soft Dice | `feature/data-v2-abl-b-dice` / `1d1a71e`，文档 `cef6b0b` | 已完成并核验；300 epoch，best 243；未通过门控 |
| C：轻量 P2Head | `feature/data-v2-abl-c-p2head` / 实际训练 `b8001b6` | 已完成并核验；300 epoch，best 282；未通过门控 |
| D1：高分辨率原型 | `feature/data-v2-abl-d-p2proto` / 实际训练 `7d277b6` | 已完成并核验；300 epoch，best 278；保留候选 |

A2、B 日志未记录实际训练 commit，表中实现提交是源码来源，不冒充训练提交证据。
B 从 `c0f4f35` 独立分叉，不包含 A1/A2。`feature/data-v2-abl-a-attention` 已确定为
总体分析分支，统一维护 A、B、C、D 知识文档、结果与阶段总结，同时保留 A 源码。
B 的 `knowledge` 文档已从 `cef6b0b` 原样复制；B 源码与配置仍位于独立 B 分支。
阶段总结见 [A、B 改进完成总结](experiment_records/data-v2-ab-summary.md)，知识索引见 [knowledge](knowledge/README.md)。

## C 独立分支

`feature/data-v2-abl-c-p2head` 从正式 Baseline `c0f4f35` 创建，只增加轻量 P2 Neck 和
四尺度 `Segment26P2Lite`，保留 P3/P4/P5 和标准 Proto。文档与已完成的 A/B 结果来自总体分析分支。
独立实现提交为 `b8001b6`；本地当前分支为 A，总体分析与 C 操作步骤已同步。
5 项本地聚焦测试及官方权重迁移通过，fused Params=23,757,752、GFLOPs@640=133.213389。
正式 Run ID 为 `data-v2-abl-001-p2head-b16-s42`，用户跳过 10 epoch 预检，在 tmux 完成全部 300 epoch。
云端训练前切到 C 分支，本地接收结果后切回 A 总体分析分支；具体命令见 `实验步骤.md`。
完整 Run 已核验，300 轮 CSV 和权重均为有限值；实际训练 commit 由日志确认。C 的正式主指标下降，未通过门控；分析见 [C 正式记录](experiment_records/runs/data-v2-abl-001-p2head-b16-s42.md)。

## 固定实验信息

| 项目 | 固定值 |
|---|---|
| 任务 | 无人机航拍水稻害虫实例分割 |
| 数据集 | `rice-pest-data-v2` |
| 类别 / 实例数 | Rice leaffolder：4027；Rice stemborers：1110 |
| 图片数 train / val / test | 938 / 117 / 118 |
| GPU | RTX 5090 |
| 统一训练配方 | P2 已冻结；epochs=300、patience=100、batch=16、imgsz=640、seed=42、AdamW、mask_ratio=2、mixup=0 |
| 模型选择 | Val official fitness = Box mAP50-95 + Mask mAP50-95 |
| 论文主指标 | Val Mask mAP50-95；同时报告 Mask mAP50 |
| Test | 已在最终模型冻结后完成一次统一评估；000 0.33490 → DSS 0.35216 |

## 已核验正式结果

| 结果 | 000 | A1 | A2 | B | C | D1 | E | F | F2 | G | DRB/SCSA |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Official fitness | 0.81977 | 0.80644 | 0.82146 | 0.80359 | 0.80954 | 0.82836 | 0.80522 | 0.81843 | 0.79673 | 0.81416 | 0.72224 |
| Mask mAP50 | 0.71132 | 0.70884 | 0.71506 | 0.68016 | 0.70130 | 0.71245 | 0.69353 | 0.70420 | 0.70155 | 0.69628 | 0.67664 |
| Mask mAP50-95 | 0.36348 | 0.35097 | 0.36109 | 0.34625 | 0.35116 | 0.37327 | 0.34649 | 0.35882 | 0.34733 | 0.36020 | 0.31719 |
| 实际 epoch | 300 | 300 | 276 | 300 | 300 | 300 | 300 | 300 | 204 | 300 | 300 |
| Best epoch | 216 | 235 | 176 | 243 | 282 | 278 | 264 | 237 | 104 | 206 | 231 |
| 门控 | 基准 | 未通过 | 近似持平 | 未通过 | 未通过 | 正向候选 | 未通过 | 未通过 | 未通过 | 未通过 | 未通过 |

F2 的实际 epoch 为 204，是 patience=100 触发的早停（best epoch 104），不是 Run 中断；其余十组均跑满或按同一 patience 正常结束。
除 D1 为正向候选、A2 与 000 近似持平外，上表其余九组均未通过门控。**DSS 复合（最终模型）**在结构层面超越单模块路线：
`data-v2-cmp1-dss-b16-s42`（DSEM + SFCM + DPRM），300 epoch / best 219，Official fitness 0.85020、
Mask mAP50 0.71687、Mask mAP50-95 **0.38929**（较 000 +2.58 pp），四组留一消融（nodual 0.34829、
nodsem 0.35796、nop2inj 0.37797、nosfcm 0.38135）全部低于完整复合且无死组件，已冻结为最终模型。
Test 终评一次（云端 GPU 官方协议）：000 0.33490 → DSS **0.35216（+1.73 pp）**，本地 CPU 配对交叉
确认 +1.84 pp。详见 [DSS 正式记录](experiment_records/runs/data-v2-cmp1-dss-b16-s42.md)、
[Test 终评记录](knowledge/Test终评记录-data-v2-cmp1.md) 与总表表 15、表 22。

A2 相对 `000` 近似持平，卷叶螟 Mask mAP50 由 0.677 降到 0.655，暂不进入注意力组合。
其 CSV 与 best.pt 元数据一致，仅 epoch 3 出现部分 Val loss NaN；正常早停，非 Run 缺失。
B 的 CSV 与 best.pt 元数据一致，实际参数仅增加两个 Dice 参数；两项 Mask AP 和两个
类别 AP 均下降，按门控淘汰本次 B 实现。上表各组总体均采用最佳 epoch 精确值，分类别为
训练结束 best.pt 复核日志值。B 的 epoch 4、6 有部分 Val loss NaN，训练指标与权重有限。
E、F、F2、G、DRB/SCSA 的正式记录、`comparison.csv` 与总表登记均已建立。

C 的小目标补充评估在本机固定同一协议执行：174 个小卷叶螟，AP50 / AP50-95 下降
0.04444 / 0.01964；固定 conf=0.25 的 Recall 提高 0.02874，正确检出增加 5 个、误检增加
26 个。召回局部收益不足以通过综合门控。详见 [专项评估](experiment_records/evaluations/data-v2-c-small-val.md)，不与正式日志混用。

## 本地文件与记录

- A2 完整原始结果：`runs/data-v2-abl-a2-p3-zrcbam-b16-s42/`，含 args、CSV、日志、权重和图表。
- A2 正式分析：`experiment_records/runs/data-v2-abl-a2-p3-zrcbam-b16-s42.md`，已写入 `comparison.csv` 与总表。
- B 完整原始结果：`runs/data-v2-abl-010-dice-b16-s42/`，共 29 个文件，含 args、CSV、权重、图表和 `data-v2-abl-010-dice-b16-s42.log`。
- B 正式分析：`experiment_records/runs/data-v2-abl-010-dice-b16-s42.md`；已写入 `comparison.csv` 与总表。
- C 完整结果：`runs/data-v2-abl-001-p2head-b16-s42/`，29 个文件；[正式记录](experiment_records/runs/data-v2-abl-001-p2head-b16-s42.md) 已写入 `comparison.csv` 与总表。
- C 与 000 的配对小目标结果：`experiment_records/evaluations/data-v2-c-small-val.json` 及同名 Markdown。
- F2 完整原始结果：`runs/data-v2-abl-f2-localgate-b16-s42/`；[正式记录](experiment_records/runs/data-v2-abl-f2-localgate-b16-s42.md) 已写入 `comparison.csv` 与总表。
- DSS 复合与四组留一消融：`runs/data-v2-cmp1-dss-b16-s42/`、`runs/data-v2-cmp2-{nodual,nodsem,nop2inj,nosfcm}-b16-s42/`；汇总见 `knowledge/消融实验设计与登记表.md`。
- Test 终评与 CPU 配对证据：`experiment_records/evaluations/test-eval-*.{json,md}` 与 `data-v2-cmp{1,2-*}-cpu-pair.json`。
- 数据、完整 Run、权重、ZIP 和本机环境不进入 Git；历史结果保持原位。

## 下一步操作

1. 保留 000、A1、A2、B、C、D1、E、F、F2、G、DRB/SCSA 与 DSS 复合及其四组留一消融的完整 Run 与正式记录；
   其中 D1 为单模块阶段唯一正向候选，最终模型为 DSS 复合，A1/A2/B/C/E/F/F2/G/DRB 均不进入组合。
2. 实验阶段收官：结构与配方已冻结，Test 终评已完成（000 0.33490 → DSS 0.35216），不再新增训练。
3. 若后续要重新设计模块，须提出独立假设并使用新 Run ID；当前不自动启动长训。
4. 剩余可选项：seed 2/3 复验、D1 独立对照行、统一效率测速（总表表 10 延迟列）；下一步工作为论文写作。

`实验步骤.md` 保存 G（P3 定向条带上下文）的云端操作流程，可作为同类结构消融的复用模板；DSS 复合与四组留一消融的操作与登记见 `knowledge/复合结构改进设计方案-DSS三位置复合.md` 与 `knowledge/消融实验设计与登记表.md`。D1 操作记录保存在总体分析分支 `631a932`，C 操作记录保存在 Git 历史中，A2 的历史步骤保存在 Git 历史中，B 原操作说明保存在
独立 B 分支。当前不重复启动已有 Run ID；切换分支前保存已有修改。

## 环境

上一台电脑的分析使用 `D:/tool/Anaconda3/envs/yolo26/python.exe`，Python 3.10.19、PyTorch 2.10.0+cu130，
通过独立 D checkout 导入 Ultralytics 8.4.80。4 项注意力测试、依赖检查和 A2 配置 dry-run 已通过。
云端正式 Run 实测为 Python 3.12.3、PyTorch 2.12.1+cu130、Ultralytics 8.4.80；本机环境
用于开发与结果分析，不能替代云端正式复现实验环境记录。
